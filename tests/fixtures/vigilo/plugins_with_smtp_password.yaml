# Unsupported plaintext credential: smtp_password
outputs:
  email-ops:
    module: "app.plugins.outputs.email"
    class: "EmailPlugin"
    config:
      smtp_host: "smtp.example.com"
      smtp_port: 587
      from_address: "vigilo@example.com"
      to_addresses:
        - "ops@example.com"
      smtp_password: "secret"
