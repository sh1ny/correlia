# Unsupported top-level section: inputs
outputs:
  email-ops:
    module: "app.plugins.outputs.email"
    class: "EmailPlugin"
    config:
      smtp_host: "smtp.example.com"
      smtp_port: 587
      from_address: "vigilo@example.com"
      to_addresses:
        - "ops@example.com"

inputs:
  icinga2:
    module: "app.plugins.inputs.icinga2"
    class: "Icinga2Plugin"
