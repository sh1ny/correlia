# Unsupported top-level section: processor
outputs:
  email-ops:
    module: "app.plugins.outputs.email"
    class: "EmailPlugin"
    config:
      smtp_host: "smtp.example.com"
      smtp_port: 587
      from_address: "vigilo@example.com"
      to_addresses:
        - "ops@example.com"

processor:
  enabled: true
  module: "app.plugins.processors.llm"
  class: "LLMProcessorPlugin"
