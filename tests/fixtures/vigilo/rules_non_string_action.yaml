# Unsupported: non-string action entry
rules:
  - name: "Bad Action Rule"
    priority: 100
    match:
      severity: ["CRITICAL"]
      host: "*"
    window:
      duration_seconds: 1800
      group_by: ["host"]
      trigger_threshold: 1
    output_summary: "Bad action"
    actions:
      - name: "email-ops"
