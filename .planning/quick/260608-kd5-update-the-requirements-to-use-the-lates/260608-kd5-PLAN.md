---
quick_id: 260608-kd5
mode: quick
title: Update documented Python requirement to latest stable bugfix-supported CPython line
autonomous: true
skip: [ROADMAP.md, tests, lint, format, commit]
research: .planning/quick/260608-kd5-update-the-requirements-to-use-the-lates/260608-kd5-RESEARCH.md
---

<objective>
Update Correlia's active Python baseline from Python 3.13+ to Python 3.14+.
Use the research finding that CPython has no separate LTS line: Python 3.14 is the latest stable bugfix-supported feature release as of 2026-06-08, while Python 3.15 is still prerelease.

Scope: docs/requirements baseline only, unless current Python package/config files directly encode the runtime baseline.
</objective>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/quick/260608-kd5-update-the-requirements-to-use-the-lates/260608-kd5-RESEARCH.md

Research constraints:
- Requirement floor: `Python 3.14+`, not patch-level `>=3.14.5`.
- Wording: do not call it "latest LTS"; say "latest stable CPython feature release in bugfix support" or just "Python 3.14+".
- Do not target Python 3.15.
- Research found no root `pyproject.toml`, `.python-version`, `requirements.txt`, `uv.lock`, source package, or CI workflow at planning time.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm active Python baseline references</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/research/STACK.md, CLAUDE.md, idea.md</files>
  <action>Search/read active docs for `Python 3.13`, `Python 3.13+`, `3.13.x baseline`, and `py313`. Also check whether Python package/config files now exist (`pyproject.toml`, `.python-version`, `requirements.txt`, `uv.lock`, CI workflow files). Include package/config files in the edit set only if they directly encode Python 3.13, `py313`, or a Python runtime floor. Do not inspect or edit `.planning/ROADMAP.md`.</action>
  <verify><automated>Search/read results identify every active Python 3.13 / py313 baseline reference and confirm whether runtime package/config files exist.</automated></verify>
  <done>Executor has the exact edit set; no Python runtime baseline file remains uninspected.</done>
</task>

<task type="auto">
  <name>Task 2: Cut active baseline over to Python 3.14+</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/research/STACK.md, CLAUDE.md, idea.md</files>
  <action>Update `.planning/REQUIREMENTS.md` `FND-01` to require Python 3.14. Update `.planning/PROJECT.md`, `idea.md`, and generated/mirrored `CLAUDE.md` GSD sections from Python 3.13+ to Python 3.14+, keeping generated markers intact. Update `.planning/research/STACK.md` and the mirrored `CLAUDE.md` stack section so the core Python row recommends a 3.14 baseline, compatibility notes discuss Python 3.14, and Ruff target guidance is `py314` instead of `py313`. If Task 1 found runtime metadata, update only the matching baseline fields: `requires-python = ">=3.14"`, `.python-version` to `3.14.5`, tool target `py314`, and CI Python version `3.14`. Do not introduce Python 3.15.</action>
  <verify><automated>Search confirms no stale active `Python 3.13`, `3.13.x baseline`, or `py313` baseline remains outside intentionally historical research/source citations.</automated></verify>
  <done>Active project documentation and any existing runtime metadata consistently use Python 3.14+ / py314.</done>
</task>

<task type="auto">
  <name>Task 3: Verify wording and execution boundaries</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/research/STACK.md, CLAUDE.md, idea.md</files>
  <action>Review changed text against the research. Ensure active docs do not call Python 3.14 "LTS", do not set the baseline to Python 3.15, and do not use `>=3.14.5` as the requirement floor. Do not run tests, lint, formatters, or commits.</action>
  <verify><automated>Search confirms active docs contain no Python "latest LTS" wording, no Python 3.15 baseline, and no `>=3.14.5` requirement floor.</automated></verify>
  <done>Change remains a docs/requirements baseline update; ROADMAP.md, tests, lint, format, and commit are untouched.</done>
</task>

</tasks>

<success_criteria>
- `.planning/REQUIREMENTS.md` FND-01 requires Python 3.14.
- Active docs/guidance consistently say Python 3.14+.
- Tooling guidance uses `py314` where a target version is documented.
- No active requirement calls Python 3.14 "LTS" or selects prerelease Python 3.15.
- No ROADMAP.md update, tests, lint, format, or commit.
</success_criteria>
