---
phase: "07-incident-event-audit-trail"
plan: "03"
type: "execute"
wave: 3
depends_on:
  - "07-02"
files_modified:
  - "app/api/routers/audit.py"
  - "app/middleware/classification.py"
  - "app/main.py"
  - "tests/test_audit_api.py"
  - "tests/test_rate_limit.py"
  - "tests/test_size_limit.py"
autonomous: true
requirements: [AUD-02, AUD-03, AUD-04]
user_setup: []
must_haves:
  truths:
    - "AUD-04/D-09: Operators can call canonical GET /v1/incident-events; no incident-scoped alias is added in this plan."
    - "AUD-02/AUD-04/D-10/D-11/D-12: The endpoint lists persisted audit rows with cursor-first pagination on (accepted_at, id) and supports incident_id, has_incident, fingerprint, source_id, event_type, incident_effect, no_dispatch_reason, severity, host, service, accepted_since/accepted_until, and event_timestamp_since/event_timestamp_until filters."
    - "AUD-02/D-08/D-15: Default read responses expose bounded metadata plus normalized_event_message and normalized_event_tags only; raw_payload and the full normalized_event object are not selected or serialized."
    - "AUD-03/AUD-04/D-12: has_incident=false maps strictly to incident_effect='none', while below_threshold and replay remain no_dispatch_reason values on incident-correlated rows."
    - "D-16: /v1/incident-events is explicitly classified as route class operator and is covered by the existing operator auth, size-limit, and rate-limit behavior."
  artifacts:
    - path: "app/api/routers/audit.py"
      provides: "Read-only operator audit-event router and bounded response mapping."
      exports:
        - "router"
        - "audit_event_list_filters"
        - "list_incident_events_endpoint"
      contains: "APIRouter(prefix='/v1/incident-events', dependencies=[Security(require_operator_token)]), _audit_event_response"
    - path: "app/middleware/classification.py"
      provides: "Explicit operator route classification for the audit endpoint."
      contains: "('/v1/incident-events', 'operator')"
    - path: "app/main.py"
      provides: "FastAPI app factory registration for the audit router."
      contains: "audit_router, app.include_router(audit_router)"
    - path: "tests/test_audit_api.py"
      provides: "Testcontainers-backed endpoint coverage for auth, bounded projection, filters, cursor pagination, incident correlation, and no-op inspection."
      contains: "test_incident_events_requires_operator_token, test_list_incident_events_uses_bounded_projection, test_list_incident_events_filters, test_list_incident_events_cursor_pagination, test_ingress_incident_and_noop_events_are_queryable"
    - path: "tests/test_rate_limit.py"
      provides: "Integration proof that /v1/incident-events inherits operator rate limiting."
      contains: "test_incident_events_inherit_operator_rate_limit"
    - path: "tests/test_size_limit.py"
      provides: "Route-classification regression for /v1/incident-events."
      contains: "('/v1/incident-events', 'operator')"
  key_links:
    - from: "app/api/routers/audit.py"
      to: "app.domain.audit"
      via: "Filter dependency constructs AuditEventListFilters and endpoint returns AuditEventListResponse/AuditEventResponse."
      pattern: "AuditEventListFilters"
    - from: "app/api/routers/audit.py"
      to: "app.persistence.audit"
      via: "list_incident_events_endpoint passes filters and AsyncSession to list_incident_events; repository owns cursor/filter SQL and bounded projection."
      pattern: "list_incident_events"
    - from: "app/api/routers/audit.py"
      to: "app.api.security"
      via: "router-level Security(require_operator_token) protects every audit read."
      pattern: "require_operator_token"
    - from: "app/main.py"
      to: "app/api/routers/audit.py"
      via: "create_app imports audit_router and includes it alongside other operator routers."
      pattern: "include_router(audit_router)"
    - from: "app/middleware/classification.py"
      to: "app.middleware.rate_limit.RateLimiterMiddleware"
      via: "classify_path('/v1/incident-events') returns operator, so operator rate-limit config and token identity buckets apply."
      pattern: "ROUTE_CLASS_PREFIXES"
---

<objective>
Expose the Phase 7 read-only operator audit surface for plan 07-03: canonical `GET /v1/incident-events`, required query filters, cursor-first pagination, bounded response projection, explicit operator route classification, FastAPI app registration, and integration coverage for incident-correlated and no-op accepted events.

Purpose: Operators need to inspect accepted event audit rows and correlate them to incidents without exposing raw payloads or letting audit data feed back into incident decisions.

Output: One new audit router, two wiring changes, and focused tests proving AUD-02, AUD-03, AUD-04 plus D-08, D-09, D-10, D-11, D-12, D-15, and D-16 for the read API layer.
</objective>

<execution_context>
@.omp/gsd-core/workflows/execute-plan.md
@.omp/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-incident-event-audit-trail/07-PLAN-OUTLINE.md
@.planning/phases/07-incident-event-audit-trail/07-CONTEXT.md
@.planning/phases/07-incident-event-audit-trail/07-RESEARCH.md
@.planning/phases/07-incident-event-audit-trail/07-PATTERNS.md
@app/api/routers/incidents.py
@app/api/deps.py
@app/api/security.py
@app/middleware/classification.py
@app/middleware/rate_limit.py
@app/domain/audit.py
@app/persistence/audit.py
@app/main.py
@tests/test_incidents_api.py
@tests/test_rate_limit.py
@tests/test_size_limit.py
</context>

## Dependency contract

This plan depends on 07-02, which itself depends on 07-01. Treat the following as already provided when executing 07-03; do not recreate their foundation work here:

- `app.domain.audit.AuditEventListFilters`
- `app.domain.audit.AuditEventListResponse`
- `app.domain.audit.AuditEventResponse`
- `app.domain.audit.AuditDecisionSummary`
- `app.persistence.audit.list_incident_events`
- `app.persistence.audit.encode_audit_cursor`
- `app.persistence.audit.decode_audit_cursor`
- `app.persistence.audit.insert_incident_event`
- `app.persistence.models.IncidentEvent`
- Audit-write integration from ingress that inserts one row per accepted normalized event from 07-02.

If any of these names differ after 07-01/07-02 execution, adapt imports to the actual exported symbol with the same contract; do not add migration, redaction, HMAC, transaction-refactor, or audit-write foundation work to this plan.

## Artifacts this phase produces

Paths created:
- `app/api/routers/audit.py`
- `tests/test_audit_api.py`

Files modified:
- `app/middleware/classification.py`
- `app/main.py`
- `tests/test_rate_limit.py`
- `tests/test_size_limit.py`

Router symbols created in `app/api/routers/audit.py`:
- `router`: `APIRouter` with prefix `/v1/incident-events` and router-level `Security(require_operator_token)` dependency.
- `audit_event_list_filters`: query dependency that constructs `AuditEventListFilters`.
- `_audit_event_response`: private row-to-`AuditEventResponse` mapper that bounds `normalized_event_message`, defaults missing tags to `{}`, validates `AuditDecisionSummary`, and never handles `raw_payload` or full `normalized_event`.
- `list_incident_events_endpoint`: async GET handler for `GET /v1/incident-events` returning `AuditEventListResponse`.

Endpoint decorators created:
- `@router.get("", response_model=AuditEventListResponse)` on `list_incident_events_endpoint`.

Query parameters accepted by `audit_event_list_filters`:
- `incident_id: UUID | None`
- `has_incident: bool | None`
- `fingerprint: str | None`
- `source_id: str | None`
- `event_type: EventType | None`
- `incident_effect: Literal['none', 'inserted', 'updated', 'resolved', 'affected_set_shrunk'] | None`
- `no_dispatch_reason: str | None`
- `severity: Severity | None`
- `host: str | None`
- `service: str | None`
- `accepted_since: datetime | None`
- `accepted_until: datetime | None`
- `event_timestamp_since: datetime | None`
- `event_timestamp_until: datetime | None`
- `limit: int` bounded to 1..200, default 50
- `cursor: str | None` bounded to 1..512 characters
- `offset: int | None` bounded to `>= 0` for diagnostic offset/total support inherited from the persistence contract.

Wiring created or changed:
- `ROUTE_CLASS_PREFIXES` gains `("/v1/incident-events", "operator")`.
- `app.main` imports `router as audit_router` from `app.api.routers.audit`.
- `create_app` calls `app.include_router(audit_router)` immediately after `app.include_router(incidents_router)`.

Test functions created in `tests/test_audit_api.py`:
- `_run_alembic_upgrade`
- `postgres_url`
- `session_factory`
- `NoopLifecycleWorker`
- `get_client`
- `_settings`
- `_app`
- `_event_time`
- `_seed_audit_event`
- `test_incident_events_requires_operator_token`
- `test_list_incident_events_empty_with_operator_token`
- `test_list_incident_events_uses_bounded_projection`
- `test_list_incident_events_filters_by_incident_and_noop_fields`
- `test_list_incident_events_filters_by_scalar_and_time_ranges`
- `test_list_incident_events_cursor_pagination_and_invalid_cursor`
- `test_ingress_incident_and_noop_events_are_queryable`

Test functions or cases created in existing files:
- `tests/test_rate_limit.py::test_incident_events_inherit_operator_rate_limit`
- `tests/test_size_limit.py` parametrize cases for `("/v1/incident-events", "operator")` and `("/v1/incident-events/{id-like-suffix}", "operator")` or equivalent prefix coverage.

Packages, migrations, CLI flags, dataclasses, settings fields, external services, and TypeScript schema-push tasks created: none.

## Source Coverage Audit

| SOURCE | ID | Feature or decision | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 7 | Maintainers and operators can trace accepted source events through normalization and incident decisions without using audit rows as decision state. | 07-03 | COVERED | Read-only operator inspection is provided; audit writes and decision isolation are upstream in 07-01/07-02 and verified here through read-side correlation tests. |
| REQ | AUD-02 | Audit rows exist for accepted normalized events with traceability fields. | 07-03 | COVERED | Endpoint exposes persisted rows from 07-02 and tests prove accepted ingress events are queryable without adding write-path logic here. |
| REQ | AUD-03 | Audit rows are not used to decide aggregation, notification thresholds, recovery, or lifecycle transitions. | 07-03 | COVERED | This plan adds only a read-only router and tests; no manager, lifecycle, rule-engine, or ingress decision code is read from audit rows. |
| REQ | AUD-04 | Operators can correlate audit rows to incidents and inspect no-op accepted events. | 07-03 | COVERED | Filters, response projection, and integration tests cover `incident_id`, `has_incident`, `incident_effect`, `no_dispatch_reason`, incident-correlated rows, and no-op rows. |
| RESEARCH | R-API-01 | Operator endpoint is `GET /v1/incident-events` with `require_operator_token`. | 07-03 | COVERED | Task 1 creates the router; Task 2 registers it. |
| RESEARCH | R-API-02 | Cursor pagination mirrors incident-list style on `(accepted_at, id)`. | 07-03 | COVERED | Task 1 delegates to `list_incident_events`; Task 3 proves page 1/page 2 cursor behavior and invalid cursor handling. |
| RESEARCH | R-API-03 | JSONB incident-id containment, scalar filters, no_dispatch_reason, and time-range filters are required. | 07-03 | COVERED | Task 1 exposes every filter; Task 3 tests representative scalar, time, incident, has_incident, and no_dispatch_reason filters. |
| RESEARCH | R-API-04 | Default read projection omits `raw_payload` and full `normalized_event`. | 07-03 | COVERED | Task 1 maps repository projection only; Task 3 asserts response JSON lacks both keys. |
| RESEARCH | R-API-05 | Route class registration drives auth/rate-limit inheritance. | 07-03 | COVERED | Task 2 adds classification and main wiring; Task 3 adds rate-limit integration coverage. |
| CONTEXT | D-08 | Default audit read responses never expose raw payload or full normalized event; message and tags are bounded/redacted on read. | 07-03 | COVERED | Task 1 mapper and Task 3 bounded projection test. |
| CONTEXT | D-09 | Canonical read surface is `GET /v1/incident-events`; no incident-scoped alias in Phase 7. | 07-03 | COVERED | Task 1 creates only this route; acceptance criteria forbid alias routes. |
| CONTEXT | D-10 | Pagination is cursor-first on immutable `(accepted_at, id)`; offset/total is diagnostic. | 07-03 | COVERED | Task 1 delegates cursor branch to repository; Task 3 verifies cursor behavior. |
| CONTEXT | D-11 | Filters include incident, correlation, decision, severity, host/service, accepted, and event timestamp dimensions. | 07-03 | COVERED | Task 1 names every query parameter; Task 3 covers them. |
| CONTEXT | D-12 | `has_incident=false` maps strictly to `incident_effect='none'`; below_threshold and replay are no_dispatch_reason values. | 07-03 | COVERED | Task 1 passes filters to repository; Task 3 asserts no-op and below-threshold/replay separation. |
| CONTEXT | D-15 | Read responses project bounded metadata only and never select or serialize raw payload or full normalized event by default. | 07-03 | COVERED | Task 1 relies on repository projection and maps only bounded fields. |
| CONTEXT | D-16 | `/v1/incident-events` is explicitly route class `operator`. | 07-03 | COVERED | Task 2 adds tuple entry and tests. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the read-only incident-events router</name>
  <files>app/api/routers/audit.py</files>
  <read_first>
    @.planning/phases/07-incident-event-audit-trail/07-CONTEXT.md
    @.planning/phases/07-incident-event-audit-trail/07-RESEARCH.md
    @.planning/phases/07-incident-event-audit-trail/07-PATTERNS.md
    @app/api/routers/incidents.py
    @app/api/deps.py
    @app/api/security.py
    @app/domain/audit.py
    @app/persistence/audit.py
  </read_first>
  <behavior>
    - RED expectation for Task 3 tests: unauthenticated access to `GET /v1/incident-events` is rejected when API auth is enabled, and authenticated access returns an `AuditEventListResponse` envelope.
    - RED expectation for Task 3 tests: the filter dependency accepts every D-11 query parameter, passes values to `AuditEventListFilters`, and rejects invalid bounds or naive datetimes through the domain model and FastAPI validation.
    - RED expectation for Task 3 tests: invalid cursors raised by `list_incident_events` become `400 {"detail":"invalid cursor"}` like `GET /v1/incidents`.
    - RED expectation for Task 3 tests: response items never include `raw_payload` or full `normalized_event`, and include only `normalized_event_message` and `normalized_event_tags` from the bounded projection per D-08 and D-15.
  </behavior>
  <action>Create `app/api/routers/audit.py` with `from __future__ import annotations`. Mirror `app/api/routers/incidents.py` structure: import `APIRouter`, `Depends`, `HTTPException`, `Query`, `Security`, `status`, `Annotated`, `datetime`, `Literal`, `UUID`, `AsyncSession`, and `async_sessionmaker`; import `get_sessionmaker`, `require_operator_token`, `EventType`, `Severity`, `AuditDecisionSummary`, `AuditEventListFilters`, `AuditEventListResponse`, `AuditEventResponse`, and `list_incident_events`. Define `router = APIRouter(prefix="/v1/incident-events", dependencies=[Security(require_operator_token)])` per D-09 and D-16. Define `audit_event_list_filters` with concrete parameters for `incident_id`, `has_incident`, `fingerprint`, `source_id`, `event_type`, `incident_effect`, `no_dispatch_reason`, `severity`, `host`, `service`, `accepted_since`, `accepted_until`, `event_timestamp_since`, `event_timestamp_until`, `limit`, `cursor`, and `offset`; use the same `Query(min_length=1, max_length=256)`, `Query(min_length=1, max_length=128)`, `Query(ge=1, le=200)`, `Query(min_length=1, max_length=512)`, and `Query(ge=0)` bounds specified in `07-PATTERNS.md`. Return `AuditEventListFilters` and let its Pydantic validators enforce timezone-aware datetime rules. Define private `_audit_event_response(row: tuple) -> AuditEventResponse` that unpacks exactly the projected row shape from `list_incident_events`, validates `decision_summary` with `AuditDecisionSummary.model_validate`, converts `event_type` to `EventType`, converts `severity` to `Severity`, coerces `incident_ids` to a tuple, turns a missing projected message into `""`, truncates `normalized_event_message` to 512 characters, and turns missing `normalized_event_tags` into `{}`. Do not reference, select, log, or serialize `raw_payload`; do not serialize the full `normalized_event` object. Define `@router.get("", response_model=AuditEventListResponse)` handler `list_incident_events_endpoint` that opens `async with sessionmaker() as session`, calls `list_incident_events(session, filters)`, maps `ValueError` to `HTTPException(status_code=400, detail="invalid cursor")`, and returns `AuditEventListResponse(items=tuple(_audit_event_response(row) for row in page.events), total=page.total, limit=filters.limit, offset=page.offset, next_cursor=page.next_cursor)`. Do not create `GET /v1/incidents/{incident_id}/events`, any payload-detail endpoint, any write endpoint, any migration, or any compatibility facade.</action>
  <verify>
    <automated>uv run python -m compileall app/api/routers/audit.py</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `app/api/routers/audit.py` defines exactly one public `router` with prefix `/v1/incident-events` and `dependencies=[Security(require_operator_token)]`.
    - Source assertion: `audit_event_list_filters` exposes all D-11 filters: `incident_id`, `has_incident`, `fingerprint`, `source_id`, `event_type`, `incident_effect`, `no_dispatch_reason`, `severity`, `host`, `service`, `accepted_since`, `accepted_until`, `event_timestamp_since`, and `event_timestamp_until`, plus `limit`, `cursor`, and diagnostic `offset`.
    - Source assertion: `list_incident_events_endpoint` delegates filtering, cursor pagination, and bounded SQL projection to `app.persistence.audit.list_incident_events`; it does not query `IncidentEvent` directly.
    - Source assertion: `_audit_event_response` constructs `AuditEventResponse` from projected fields only; the strings `raw_payload=` and `normalized_event=` are absent from response construction.
    - Behavior assertion: an invalid cursor from persistence returns status 400 with JSON body `{"detail":"invalid cursor"}`.
    - Behavior assertion: no incident-scoped alias such as `/v1/incidents/{incident_id}/events` is added.
  </acceptance_criteria>
  <done>D-08, D-09, D-10, D-11, D-12, and D-15 are represented at the router boundary, with the actual query and projection contract delegated to 07-01/07-02 persistence symbols.</done>
</task>

<task type="auto">
  <name>Task 2: Register the router and classify the route as operator</name>
  <files>app/middleware/classification.py, app/main.py, tests/test_size_limit.py</files>
  <read_first>
    @.planning/phases/07-incident-event-audit-trail/07-CONTEXT.md
    @.planning/phases/07-incident-event-audit-trail/07-PATTERNS.md
    @app/middleware/classification.py
    @app/main.py
    @tests/test_size_limit.py
  </read_first>
  <action>In `app/middleware/classification.py`, add `("/v1/incident-events", "operator")` to `ROUTE_CLASS_PREFIXES` near the existing `("/v1/incidents", "operator")` entry per D-16; do not change `RouteClass` or the `classify_path` loop. In `app/main.py`, import `router as audit_router` from `app.api.routers.audit` beside the incidents router import, and call `app.include_router(audit_router)` immediately after `app.include_router(incidents_router)` so operator routers remain adjacent. In `tests/test_size_limit.py`, extend the existing `test_classify_path_maps_routes` parametrization with `/v1/incident-events` and a nested path such as `/v1/incident-events/example-id`, both expected to be `operator`. Preserve any `audit_raw_payload_hmac_key` arguments or settings-helper changes introduced by 07-01; if adding a new `Settings(...)` call in this file, include the audit HMAC key required by 07-01.</action>
  <verify>
    <automated>uv run pytest tests/test_size_limit.py::test_classify_path_maps_routes -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `ROUTE_CLASS_PREFIXES` contains a literal entry for `("/v1/incident-events", "operator")`; classification for this route is explicit and not relying on the default `operator` fallback.
    - Source assertion: `app/main.py` imports `audit_router` and includes it exactly once in `create_app`.
    - Behavior assertion: `classify_path("/v1/incident-events") == "operator"`.
    - Behavior assertion: `classify_path("/v1/incident-events/example-id") == "operator"`.
    - Source assertion: no new middleware, route class, auth dependency, or settings field is introduced in this task.
  </acceptance_criteria>
  <done>D-16 is true in both app wiring and route-class tests, and `GET /v1/incident-events` is reachable from the FastAPI app factory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add endpoint, rate-limit, filter, cursor, and correlation integration tests</name>
  <files>tests/test_audit_api.py, tests/test_rate_limit.py</files>
  <read_first>
    @.planning/phases/07-incident-event-audit-trail/07-CONTEXT.md
    @.planning/phases/07-incident-event-audit-trail/07-RESEARCH.md
    @.planning/phases/07-incident-event-audit-trail/07-PATTERNS.md
    @app/api/routers/audit.py
    @app/api/routers/incidents.py
    @app/api/deps.py
    @app/api/security.py
    @app/middleware/rate_limit.py
    @app/main.py
    @app/domain/audit.py
    @app/persistence/audit.py
    @tests/test_incidents_api.py
    @tests/test_rate_limit.py
  </read_first>
  <behavior>
    - RED first: `test_incident_events_requires_operator_token` creates an app with API auth enabled and asserts unauthenticated `GET /v1/incident-events` returns 401 with `{"detail":"unauthorized"}`, while the operator Bearer token can access the route.
    - RED first: `test_list_incident_events_empty_with_operator_token` verifies an authenticated empty table response has `items=[]`, `total=0`, `limit=50`, `offset=0`, and `next_cursor is None`.
    - RED first: `test_list_incident_events_uses_bounded_projection` seeds a row with `raw_payload` and a `normalized_event` containing extra fields, then asserts the JSON item has `normalized_event_message` and `normalized_event_tags` but no `raw_payload` key and no `normalized_event` key.
    - RED first: `test_list_incident_events_filters_by_incident_and_noop_fields` seeds incident-correlated and no-op rows, then verifies `incident_id`, `has_incident=true`, `has_incident=false`, `incident_effect`, and `no_dispatch_reason` filters return the expected IDs; below-threshold or replay rows remain incident-correlated and are found by `no_dispatch_reason`, not by `has_incident=false`.
    - RED first: `test_list_incident_events_filters_by_scalar_and_time_ranges` verifies `fingerprint`, `source_id`, `event_type`, `severity`, `host`, `service`, `accepted_since`/`accepted_until`, and `event_timestamp_since`/`event_timestamp_until` narrow to the seeded target rows.
    - RED first: `test_list_incident_events_cursor_pagination_and_invalid_cursor` seeds five rows, verifies default descending `(accepted_at, id)` ordering with a non-null `next_cursor`, verifies page two, and verifies an invalid cursor returns 400 `{"detail":"invalid cursor"}`.
    - RED first: `test_ingress_incident_and_noop_events_are_queryable` uses the 07-02 ingress audit-write path to POST accepted source events, then verifies an incident-producing event has non-empty `incident_ids` and `incident_effect != "none"`, while a no-op accepted event has empty `incident_ids`, `incident_effect == "none"`, and is returned by `has_incident=false`.
    - RED first: `tests/test_rate_limit.py::test_incident_events_inherit_operator_rate_limit` sends two unauthenticated requests to `/v1/incident-events` with operator rate limit set to one; the first reaches auth and returns 401, the second is stopped by the operator rate limiter with 429 before route handler/database work.
  </behavior>
  <action>
    Create `tests/test_audit_api.py` by mirroring the Testcontainers, `AsyncClient`, `ASGITransport`, `_run_alembic_upgrade`, `postgres_url`, `session_factory`, `NoopLifecycleWorker`, `_settings`, and `_app` patterns from `tests/test_incidents_api.py`. Include the audit HMAC key in every `Settings(...)` helper if 07-01 made it required. Clean both `IncidentEvent` and incident tables between tests using SQLAlchemy tables from `app.persistence.models`; preserve 07-02 cleanup needs for audit rows and incidents. Seed direct read-path rows with `app.persistence.audit.insert_incident_event` so filter and pagination tests do not depend on ingress setup. Use deterministic timezone-aware datetimes and UUIDs.
    For the bounded projection test, seed a row whose stored `raw_payload` contains obvious secret-like data and whose stored `normalized_event` has message, tags, and at least one extra key; assert the API response omits only the payload/full-event bodies while preserving metadata fields such as `raw_payload_original_byte_length`, `raw_payload_stored_byte_length`, `raw_payload_truncated`, `redaction_version`, `redacted_path_count`, and `raw_payload_hmac` if those fields are part of the 07-01 response model.
    For correlation coverage, use the 07-02 ingress path rather than manually inventing audit rows: configure enough real rules/topology/plugins or minimal existing fixtures to make one accepted event create/update an incident, then send one accepted event that produces no incident; query `/v1/incident-events` with the operator token and assert incident and no-op rows are inspectable through the API.
    In `tests/test_rate_limit.py`, add `test_incident_events_inherit_operator_rate_limit` using the existing `_auth_settings`, `_app`, and `get_client` patterns; keep auth enabled and omit the Authorization header so the first request returns 401 and the second returns 429 without opening a database session. If 07-01 made `audit_raw_payload_hmac_key` mandatory, patch `_auth_settings` to pass `audit_raw_payload_hmac_key=SecretStr("test-audit-hmac")` and add `SecretStr` to the test imports if it is not already present, so existing rate-limit tests and the new audit route test do not fail during `Settings` construction. Do not modify production decision code, migrations, audit persistence, domain models, or settings in this task.
  </action>
  <verify>
    <automated>uv run pytest tests/test_audit_api.py tests/test_rate_limit.py::test_incident_events_inherit_operator_rate_limit -q</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: unauthenticated `GET /v1/incident-events` returns 401 when auth is enabled; authenticated operator-token requests can list rows.
    - Behavior assertion: empty authenticated list response returns an `AuditEventListResponse` shape with `items`, `total`, `limit`, `offset`, and `next_cursor`.
    - Behavior assertion: response items do not contain `raw_payload` or `normalized_event` keys; response items do contain bounded `normalized_event_message` and `normalized_event_tags` keys.
    - Behavior assertion: `incident_id` filtering uses a row with at least two incident IDs and returns the row when either ID is requested.
    - Behavior assertion: `has_incident=false` returns only rows with `incident_effect == "none"`; below-threshold and replay rows are not returned by `has_incident=false` unless their `incident_effect` is actually `none`.
    - Behavior assertion: filters for `fingerprint`, `source_id`, `event_type`, `incident_effect`, `no_dispatch_reason`, `severity`, `host`, `service`, accepted time range, and event timestamp range each narrow to the intended seeded row or rows.
    - Behavior assertion: cursor pagination orders by `(accepted_at DESC, id DESC)`, returns a non-empty `next_cursor` when more rows exist, and uses that cursor to fetch the next page without duplicates.
    - Behavior assertion: malformed cursor input returns 400 with compact detail `invalid cursor`.
    - Behavior assertion: an ingress accepted event that creates or updates an incident is visible through `/v1/incident-events` with non-empty `incident_ids`; an ingress accepted no-op is visible with `incident_effect == "none"` and empty `incident_ids`.
    - Behavior assertion: `/v1/incident-events` inherits operator rate limits: with operator limit one and no Authorization header, the first request returns 401 and the second request returns 429 `{"detail":"rate limit exceeded"}`.
  </acceptance_criteria>
  <done>AUD-02, AUD-03, AUD-04 and D-08 through D-12 plus D-15/D-16 are proven at the operator API boundary with Testcontainers-backed endpoint coverage and rate-limit inheritance coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External operator client -> FastAPI audit router | Untrusted query parameters and Bearer credentials reach `GET /v1/incident-events`. |
| FastAPI audit router -> Pydantic audit domain models | Query values become `AuditEventListFilters`; projected rows become `AuditEventResponse` and `AuditEventListResponse`. |
| Audit router -> SQLAlchemy audit repository | Typed filters and cursor values are passed to `list_incident_events`, which queries PostgreSQL audit rows. |
| Audit repository projection -> API response | Persisted `raw_payload` and full `normalized_event` exist in the database but must not cross the default read boundary. |
| Phase 5 middleware -> new route | Route class controls request-size and rate-limit inheritance before the router executes. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-07-03-S | Spoofing | High | `app/api/routers/audit.py` | mitigate | Use router-level `Security(require_operator_token)` exactly like `/v1/incidents`; tests assert unauthenticated callers receive 401. |
| T-07-03-T | Tampering | Medium | `audit_event_list_filters` | mitigate | Use typed FastAPI query parameters and strict `AuditEventListFilters`; no string-built SQL or raw query fragments in the router. |
| T-07-03-R | Repudiation | Low | Audit read endpoint | accept | This plan does not add read-access audit logging because Phase 7 deferred audit log safety to Phase 10; endpoint remains read-only and operator-authenticated. |
| T-07-03-I1 | Information Disclosure | High | Response projection | mitigate | `_audit_event_response` maps only projected columns; repository must omit `raw_payload` and full `normalized_event`; tests assert both keys are absent. |
| T-07-03-I2 | Information Disclosure | High | Route protection | mitigate | Add explicit operator route classification and main registration; rate-limit/auth tests exercise `/v1/incident-events` under operator controls. |
| T-07-03-D | Denial of Service | Medium | List endpoint | mitigate | Keep `limit` bounded to 200, use cursor-first pagination on `(accepted_at, id)`, and rely on Phase 5 operator rate limits; add direct rate-limit regression. |
| T-07-03-E | Elevation of Privilege | Medium | Audit read/API-to-decision boundary | mitigate | Add only a read-only GET router; no manager, lifecycle, rule-engine, ingress decision, mutation, or write endpoint code reads from audit rows. |
| T-07-03-SC | Tampering | Low | Package installs | mitigate | No npm, pip, cargo, or other package installs occur in this plan; if an executor believes a package is needed, stop and re-plan with the package legitimacy gate before installing anything. |
</threat_model>

<verification>
Run these checks after all three tasks complete:

1. `uv run python -m compileall app/api/routers/audit.py app/main.py app/middleware/classification.py`
2. `uv run pytest tests/test_size_limit.py::test_classify_path_maps_routes -q`
3. `uv run pytest tests/test_rate_limit.py::test_incident_events_inherit_operator_rate_limit -q`
4. `uv run pytest tests/test_audit_api.py -q`
5. If a targeted check fails because 07-01/07-02 renamed audit domain or persistence symbols, update only the 07-03 imports/tests to the actual exported names with equivalent contracts; do not implement 07-01/07-02 foundation in this plan.

Do not run broad project-wide tests unless the targeted checks expose a cross-file regression that cannot be isolated to this plan's files.
</verification>

<success_criteria>
- AUD-02 is true at the read boundary: accepted-event audit rows written by earlier 07 plans are visible through the operator API with source ID, fingerprint, severity, host, service, timestamps, incident correlation, decision summary, and bounded normalized metadata.
- AUD-03 remains true: this plan adds no audit-row reads to aggregation, notification threshold, recovery, lifecycle, rule-engine, or ingress decision code.
- AUD-04 is true: operators can filter by `incident_id` to correlate rows to incidents and can filter `has_incident=false` or `incident_effect=none` to inspect accepted no-op events.
- D-08 and D-15 are true: default API responses and tests prove `raw_payload` and full `normalized_event` are absent; only bounded `normalized_event_message` and `normalized_event_tags` are returned.
- D-09 is true: the canonical route is `GET /v1/incident-events`, and no incident-scoped alias is added.
- D-10 is true: cursor-first pagination uses the 07-01/07-02 `(accepted_at, id)` cursor contract and invalid cursors return compact 400 responses.
- D-11 is true: all required filters are accepted and covered by integration tests.
- D-12 is true: `has_incident=false` means `incident_effect='none'`; no-dispatch reasons such as `below_threshold` and `replay` remain queryable separately.
- D-16 is true: route classification explicitly marks `/v1/incident-events` as operator, and rate-limit/classification tests prove inheritance.
</success_criteria>

<output>
Create `.planning/phases/07-incident-event-audit-trail/07-03-SUMMARY.md` when execution of this plan is done.
</output>

## PLAN COMPLETE — 07-03
