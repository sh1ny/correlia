---
phase: "07-incident-event-audit-trail"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "migrations/versions/0003_create_incident_events.py"
  - "app/persistence/models.py"
  - "app/domain/audit.py"
  - "app/persistence/audit.py"
  - "app/config/settings.py"
  - "tests/conftest.py"
  - "tests/test_domain_audit.py"
  - "tests/test_audit_redaction.py"
  - "tests/test_audit_persistence.py"
  - "tests/test_migrations.py"
  - "tests/test_settings.py"
  - "tests/test_config_status_api.py"
  - "tests/test_exposure_config.py"
  - "tests/test_health.py"
  - "tests/test_incidents_api.py"
  - "tests/test_ingress_router.py"
  - "tests/test_lifecycle_worker.py"
  - "tests/test_metrics_api.py"
  - "tests/test_plugins_router.py"
  - "tests/test_rate_limit.py"
  - "tests/test_security.py"
  - "tests/test_size_limit.py"
autonomous: true
requirements:
  - "AUD-01"
  - "AUD-02"
user_setup: []
must_haves:
  truths:
    - "AUD-01/D-13: Alembic head creates an append-only incident_events table whose id is generated by PostgreSQL with gen_random_uuid(), not by Python caller code."
    - "AUD-01/D-04/D-07: incident_events stores non-null raw_payload plus non-null original/stored byte lengths, truncation flag, redaction_version, redacted_path_count, and raw_payload_hmac metadata."
    - "AUD-01/D-11/D-14: incident_events has lookup indexes for cursor pagination, scalar filters, JSONB incident_ids containment via GIN, and decision_summary no_dispatch_reason via a functional index."
    - "AUD-02/D-17/D-18/D-19: AuditDecisionSummary is a strict, versioned, bounded Pydantic v2 contract with exactly the decision-summary fields needed for audit and without high-cardinality rule internals."
    - "AUD-02/D-04/D-05/D-06/D-07: persistence helpers can transform an accepted source payload into a redacted, semantically size-capped payload snapshot with a keyed HMAC over the pre-redaction canonical payload."
    - "AUD-02/D-08: default audit list projection models and repository reads do not expose raw_payload or the full normalized_event document; only bounded normalized_event_message and normalized_event_tags that are redacted for sensitive keys/values, plus raw-payload metadata, are projected."
    - "AUD-02/D-06/D-07: Settings exposes CORRELIA_AUDIT_RAW_PAYLOAD_MAX_BYTES and requires CORRELIA_AUDIT_RAW_PAYLOAD_HMAC_KEY without reusing operator or ingress auth tokens."
  artifacts:
    - path: "migrations/versions/0003_create_incident_events.py"
      provides: "incident_events table, constraints, server-generated UUID, and audit lookup indexes."
      contains: "revision, down_revision, upgrade, downgrade, ix_incident_events_incident_ids_gin, ix_incident_events_no_dispatch_reason"
    - path: "app/persistence/models.py"
      provides: "SQLAlchemy IncidentEvent declarative model mapped to incident_events."
      contains: "class IncidentEvent"
    - path: "app/domain/audit.py"
      provides: "Audit decision-summary, filter, row response, and list response contracts."
      contains: "AuditDecisionSummary, AuditEventListFilters, AuditEventResponse, AuditEventListResponse"
    - path: "app/persistence/audit.py"
      provides: "Audit redaction, HMAC, cursor, insert, and list repository helpers."
      contains: "RedactedPayload, AuditEventCursor, AuditEventListRow, IncidentEventListPage, redact_payload, redact_normalized_event_message_tags, compute_payload_hmac, encode_audit_cursor, decode_audit_cursor, insert_incident_event, list_incident_events"
    - path: "app/config/settings.py"
      provides: "Audit raw-payload cap and required HMAC-key settings."
      contains: "audit_raw_payload_max_bytes, audit_raw_payload_hmac_key, _require_audit_hmac_key"
    - path: "tests/test_domain_audit.py"
      provides: "Pydantic contract coverage for AuditDecisionSummary, filters, and bounded responses."
    - path: "tests/test_audit_redaction.py"
      provides: "Pure unit coverage for recursive redaction, semantic size capping, and pre-redaction HMAC."
    - path: "tests/test_audit_persistence.py"
      provides: "Testcontainers-backed coverage for audit insert/list/cursor/filter/projection behavior."
  key_links:
    - from: "migrations/versions/0003_create_incident_events.py"
      to: "app/persistence/models.py"
      via: "IncidentEvent columns, nullability, JSONB defaults, and server UUID default mirror the migration contract."
      pattern: "IncidentEvent.__tablename__ == 'incident_events'"
    - from: "app/config/settings.py"
      to: "app/persistence/audit.py"
      via: "redact_payload uses audit_raw_payload_max_bytes and audit_raw_payload_hmac_key supplied by Settings."
      pattern: "audit_raw_payload_hmac_key"
    - from: "app/domain/audit.py"
      to: "app/persistence/audit.py"
      via: "list_incident_events accepts AuditEventListFilters and returns IncidentEventListPage rows suitable for AuditEventResponse."
      pattern: "AuditEventListFilters"
    - from: "app/persistence/audit.py"
      to: "app/persistence/models.py"
      via: "insert_incident_event constructs IncidentEvent rows and list_incident_events projects only safe columns."
      pattern: "IncidentEvent"
    - from: "tests/test_audit_persistence.py"
      to: "migrations/versions/0003_create_incident_events.py"
      via: "Testcontainers database is migrated to head before repository behavior is asserted."
      pattern: "alembic"
---

<objective>
Create the Phase 7 audit foundation for AUD-01 and AUD-02: database schema, ORM model, strict audit domain contracts, redaction/HMAC/cursor/list persistence helpers, audit settings, and focused unit/migration/persistence tests.

Purpose: Later Phase 7 plans can wire ingress and the operator API against a stable audit contract without guessing table shape, raw-payload safety semantics, cursor behavior, or response projection boundaries.

Output: `incident_events` migration/model, `app.domain.audit`, `app.persistence.audit`, audit settings, Settings callsite maintenance, and tests proving the foundation before ingress/API wiring begins.
</objective>

<execution_context>
@.omp/gsd-core/workflows/execute-plan.md
@.omp/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-incident-event-audit-trail/07-PLAN-OUTLINE.md
@.planning/phases/07-incident-event-audit-trail/07-CONTEXT.md
@.planning/phases/07-incident-event-audit-trail/07-RESEARCH.md
@.planning/phases/07-incident-event-audit-trail/07-PATTERNS.md
@app/domain/events.py
@app/domain/incidents.py
@app/persistence/models.py
@app/persistence/incidents.py
@app/config/settings.py
@migrations/env.py
@migrations/versions/0001_create_incidents.py
@migrations/versions/0002_add_threshold_state.py
@tests/conftest.py
@tests/test_domain_incidents.py
@tests/test_incident_repository.py
@tests/test_migrations.py
@tests/test_settings.py
</context>

## Artifacts this phase produces

Paths created:
- `migrations/versions/0003_create_incident_events.py`
- `app/domain/audit.py`
- `app/persistence/audit.py`
- `tests/test_domain_audit.py`
- `tests/test_audit_redaction.py`
- `tests/test_audit_persistence.py`

Files modified:
- `app/persistence/models.py`
- `app/config/settings.py`
- `tests/conftest.py`
- `tests/test_migrations.py`
- `tests/test_settings.py`
- `tests/test_config_status_api.py`
- `tests/test_exposure_config.py`
- `tests/test_health.py`
- `tests/test_incidents_api.py`
- `tests/test_ingress_router.py`
- `tests/test_lifecycle_worker.py`
- `tests/test_metrics_api.py`
- `tests/test_plugins_router.py`
- `tests/test_rate_limit.py`
- `tests/test_security.py`
- `tests/test_size_limit.py`

Database objects created:
- Table `incident_events`.
- Columns: `id`, `accepted_at`, `event_timestamp`, `source_id`, `fingerprint`, `event_type`, `severity`, `host`, `service`, `incident_ids`, `incident_effect`, `decision_summary`, `normalized_event`, `raw_payload`, `raw_payload_original_byte_length`, `raw_payload_stored_byte_length`, `raw_payload_truncated`, `redaction_version`, `redacted_path_count`, `raw_payload_hmac`.
- CHECK constraints: `ck_incident_events_event_type`, `ck_incident_events_severity`, `ck_incident_events_incident_effect`, `ck_incident_events_incident_ids_array`, `ck_incident_events_decision_summary_object`, `ck_incident_events_normalized_event_object`, `ck_incident_events_raw_payload_object`, `ck_incident_events_raw_payload_byte_lengths_non_negative`, `ck_incident_events_raw_payload_stored_lte_original`, `ck_incident_events_redaction_version_positive`, `ck_incident_events_redacted_path_count_non_negative`.
- Indexes: `ix_incident_events_accepted_at_id`, `ix_incident_events_incident_ids_gin`, `ix_incident_events_fingerprint`, `ix_incident_events_source_id`, `ix_incident_events_event_type`, `ix_incident_events_severity`, `ix_incident_events_host`, `ix_incident_events_service`, `ix_incident_events_incident_effect`, `ix_incident_events_event_timestamp`, `ix_incident_events_no_dispatch_reason`.

Settings fields created:
- `Settings.audit_raw_payload_max_bytes: int = Field(default=65_536, ge=1_024, le=1_048_576)` mapped from `CORRELIA_AUDIT_RAW_PAYLOAD_MAX_BYTES`.
- `Settings.audit_raw_payload_hmac_key: SecretStr` mapped from `CORRELIA_AUDIT_RAW_PAYLOAD_HMAC_KEY`.

Settings validators created:
- `Settings._require_audit_hmac_key` rejects missing or blank `audit_raw_payload_hmac_key` and rejects equality with either auth token when those tokens are configured.

ORM classes created:
- `app.persistence.models.IncidentEvent` with SQLAlchemy `Mapped[...]` fields matching the table columns above and `id` using `server_default=sa.func.gen_random_uuid()`.

Pydantic/type symbols created in `app/domain/audit.py`:
- `BoundedResponseMessage`
- `AuditDecisionSummary`
- `AuditEventListFilters`
- `AuditEventResponse`
- `AuditEventListResponse`

Dataclasses and constants created in `app/persistence/audit.py`:
- `REDACTION_VERSION = 1`
- `AUDIT_REDACTION_PLACEHOLDER = "[redacted]"`
- `AUDIT_SENSITIVE_FRAGMENTS`
- `RedactedPayload` with fields `payload`, `original_byte_length`, `stored_byte_length`, `truncated`, `redaction_version`, `redacted_path_count`, and `payload_hmac`.
- `AuditEventCursor` with fields `accepted_at` and `id`.
- `AuditEventListRow` with fields matching the default safe list projection.
- `IncidentEventListPage` with fields `events`, `next_cursor`, `total`, and `offset`.

Functions created in `app/persistence/audit.py`:
- `canonical_json_bytes`
- `compute_payload_hmac`
- `redact_payload`
- `redact_normalized_event_message_tags`
- `encode_audit_cursor`
- `decode_audit_cursor`
- `insert_incident_event`
- `list_incident_events`

Private helper symbols planned in `app/persistence/audit.py`:
- `_AUDIT_LIST_COLUMNS`
- `_secret_value_bytes`
- `_contains_sensitive_fragment`
- `_redact_value`
- `_semantic_cap_payload`
- `_stored_byte_length`
- `_redact_normalized_event_message_tags`

Test functions created:
- `tests/test_domain_audit.py::test_audit_decision_summary_accepts_required_fields_and_defaults_schema_version`
- `tests/test_domain_audit.py::test_audit_decision_summary_rejects_extra_fields`
- `tests/test_domain_audit.py::test_audit_decision_summary_rejects_invalid_literals`
- `tests/test_domain_audit.py::test_audit_decision_summary_bounds_incident_ids_and_strings`
- `tests/test_domain_audit.py::test_audit_event_filters_require_timezone_aware_ranges`
- `tests/test_domain_audit.py::test_audit_event_response_excludes_raw_payload_and_full_normalized_event`
- `tests/test_audit_redaction.py::test_redact_payload_redacts_sensitive_keys_and_string_values_recursively`
- `tests/test_audit_redaction.py::test_redact_payload_hmac_uses_pre_redaction_canonical_payload`
- `tests/test_audit_redaction.py::test_redact_payload_semantic_cap_preserves_json_and_sets_metadata`
- `tests/test_audit_redaction.py::test_redact_normalized_event_message_tags_redacts_sensitive_keys_and_values`
- `tests/test_audit_persistence.py::test_insert_incident_event_persists_non_null_raw_payload_metadata`
- `tests/test_audit_persistence.py::test_list_incident_events_uses_safe_projection_without_raw_payload_or_full_normalized_event`
- `tests/test_audit_persistence.py::test_audit_cursor_round_trips_and_paginates_on_accepted_at_id`
- `tests/test_audit_persistence.py::test_incident_id_filter_uses_jsonb_containment_semantics`
- `tests/test_audit_persistence.py::test_has_incident_false_maps_only_to_incident_effect_none`
- `tests/test_audit_persistence.py::test_no_dispatch_reason_filter_reads_decision_summary_field`
- `tests/test_audit_persistence.py::test_decode_audit_cursor_rejects_invalid_or_naive_values`
- New `tests/test_migrations.py` functions covering the incident_events table, columns, constraints, indexes, and server-generated id.
- New `tests/test_settings.py` functions covering audit HMAC key requirement, non-empty validation, auth-token separation, SecretStr storage, default payload cap, and env-key cleanup coverage.

Decorators, CLI flags, external services, and packages created: none.

## Source Coverage Audit

| SOURCE | ID | Feature or decision | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 7 | Trace accepted source events through normalization and incident decisions without using audit rows as decision state. | 07-01 | PARTIAL FOUNDATION | This plan creates the schema/contracts/repository needed for tracing. Ingress write timing and read API are assigned to 07-02 and 07-03 by the outline. |
| REQ | AUD-01 | Maintainers can migrate the database to include append-only `incident_events` with traceability columns and indexes. | 07-01 | COVERED | Task 1 creates migration/model and migration tests. |
| REQ | AUD-02 | Correlia records one audit row per accepted normalized event with raw payload, normalized event, decision summary, source ID, fingerprint, severity, host, service, and timestamps. | 07-01 | FOUNDATION COVERED | Tasks 1-3 create the durable row shape and insert helper. Wiring every accepted ingress path is assigned to 07-02. |
| REQ | AUD-03 | Audit rows do not influence aggregation, thresholds, recovery, or lifecycle. | 07-02 | OUT OF SCOPE | Outline assigns transaction refactor and decision-state isolation to 07-02. This plan creates no reads from audit helpers in decision modules. |
| REQ | AUD-04 | Operators correlate audit rows to incidents and inspect no-op accepted events. | 07-03 | OUT OF SCOPE | This plan creates `incident_ids`, filters, and projection foundation; the operator endpoint is assigned to 07-03. |
| RESEARCH | R-01 | Alembic migration `0003_create_incident_events` with JSONB columns, cursor index, GIN incident_ids index, scalar filter indexes, and no_dispatch_reason functional index. | 07-01 | COVERED | Task 1. |
| RESEARCH | R-02 | PostgreSQL `gen_random_uuid()` is required for audit row id. | 07-01 | COVERED | Task 1 implements D-13. |
| RESEARCH | R-03 | Strict Pydantic v2 bounded models mirror `DecisionContext` discipline. | 07-01 | COVERED | Task 2 implements D-17 through D-19. |
| RESEARCH | R-04 | Recursive raw-payload redactor with semantic cap and pre-redaction HMAC. | 07-01 | COVERED | Task 2 implements D-04 through D-07. |
| RESEARCH | R-05 | Cursor pagination mirrors `list_incidents` on immutable `(accepted_at, id)`. | 07-01 | COVERED | Task 2 creates cursor helpers; Task 3 uses them in listing. |
| RESEARCH | R-06 | Default list projection must not select raw payload or full normalized event. | 07-01 | COVERED | Task 3 implements D-08 projection foundation. |
| RESEARCH | R-07 | No new packages are needed. | 07-01 | COVERED | Tasks use existing SQLAlchemy, Alembic, Pydantic, Testcontainers, and stdlib `json`/`hmac`/`hashlib`/`base64`. |
| CONTEXT | D-04 | Store a redacted, size-capped accepted source payload object. | 07-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-05 | Raw payload redaction strips secrets with a new recursive redactor. | 07-01 | COVERED | Task 2 explicitly keeps the redactor audit-owned. |
| CONTEXT | D-06 | Raw payload size cap is configurable and semantic. | 07-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-07 | Store original/stored lengths, truncation flag, redaction version, redacted path count, and keyed HMAC. | 07-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-08 | Default audit reads never expose raw_payload or full normalized_event; message and tags are bounded/redacted on read. | 07-01 | COVERED | Tasks 2-3 create response/projection foundation including `redact_normalized_event_message_tags` helper applied to projected message and tags. |
| CONTEXT | D-13 | Audit row identifier is server-generated UUID; fingerprint is correlation only. | 07-01 | COVERED | Task 1. |
| CONTEXT | D-17 | `AuditDecisionSummary` is versioned, strict, and `extra="forbid"`. | 07-01 | COVERED | Task 2. |
| CONTEXT | D-18 | `AuditDecisionSummary` required fields are exactly the bounded decision-summary set. | 07-01 | COVERED | Task 2. |
| CONTEXT | D-19 | Exclude high-cardinality and rule-engine-internal dicts from decision summary. | 07-01 | COVERED | Task 2. |
| CONTEXT | D-01, D-02, D-03 | Audit write timing and commit ownership. | 07-02 | OUT OF SCOPE | Outline assigns to 07-02; this plan only creates helpers consumed there. |
| CONTEXT | D-09, D-10, D-11, D-12, D-14, D-15, D-16 | Operator API, filters, route classification, and incident correlation read surface. | 07-03 | OUT OF SCOPE EXCEPT FOUNDATION | This plan creates filter DTOs, `incident_ids`, and repository filters. Router/classification/API tests are 07-03. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add incident_events schema, ORM model, audit settings, and Settings-callsite maintenance</name>
  <files>migrations/versions/0003_create_incident_events.py, app/persistence/models.py, app/config/settings.py, tests/conftest.py, tests/test_migrations.py, tests/test_settings.py, tests/test_config_status_api.py, tests/test_exposure_config.py, tests/test_health.py, tests/test_incidents_api.py, tests/test_ingress_router.py, tests/test_lifecycle_worker.py, tests/test_metrics_api.py, tests/test_plugins_router.py, tests/test_rate_limit.py, tests/test_security.py, tests/test_size_limit.py</files>
  <read_first>
    @.planning/ROADMAP.md
    @.planning/REQUIREMENTS.md
    @.planning/phases/07-incident-event-audit-trail/07-CONTEXT.md
    @.planning/phases/07-incident-event-audit-trail/07-RESEARCH.md
    @.planning/phases/07-incident-event-audit-trail/07-PATTERNS.md
    @app/persistence/models.py
    @app/config/settings.py
    @migrations/env.py
    @migrations/versions/0001_create_incidents.py
    @migrations/versions/0002_add_threshold_state.py
    @tests/conftest.py
    @tests/test_migrations.py
    @tests/test_settings.py
    @tests/test_config_status_api.py
    @tests/test_exposure_config.py
    @tests/test_health.py
    @tests/test_incidents_api.py
    @tests/test_ingress_router.py
    @tests/test_lifecycle_worker.py
    @tests/test_metrics_api.py
    @tests/test_plugins_router.py
    @tests/test_rate_limit.py
    @tests/test_security.py
    @tests/test_size_limit.py
  </read_first>
  <behavior>
    - RED first: migration tests assert `incident_events` exists after `alembic upgrade head`, all required columns exist with PostgreSQL UUID/JSONB/timestamp/boolean/integer/string types, raw-payload metadata columns are non-null, JSONB object/array and non-negative metadata CHECK constraints exist, and every named index exists.
    - RED first: migration tests insert an audit row without an explicit `id` but with all required non-null audit fields and assert PostgreSQL returns a non-null UUID generated by `gen_random_uuid()`.
    - RED first: settings tests assert `audit_raw_payload_hmac_key` is required even when `api_auth_enabled=False`, blank keys fail validation, the audit key differs from configured operator and ingress tokens, the key is stored as `SecretStr`, and `audit_raw_payload_max_bytes` defaults to 65_536.
    - RED first: a settings-callsite regression should fail until every existing test `Settings(...)` factory/callsite and env fixture has a test audit HMAC key.
  </behavior>
  <action>Create `migrations/versions/0003_create_incident_events.py` with `revision = "0003_create_incident_events"` and `down_revision = "0002_add_threshold_state"`. In `upgrade()`, create table `incident_events` with PostgreSQL-generated `id` using `server_default=sa.text("gen_random_uuid()")`, `accepted_at` server default `sa.func.now()`, required source fields (`event_timestamp`, `source_id`, `fingerprint`, `event_type`, `severity`, `host`, nullable `service`), required JSONB fields (`incident_ids`, `decision_summary`, `normalized_event`, non-null `raw_payload` with empty-object default), required raw metadata fields (`raw_payload_original_byte_length`, `raw_payload_stored_byte_length`, `raw_payload_truncated`, `redaction_version`, `redacted_path_count`, `raw_payload_hmac`), and CHECK constraints named in the artifact list. Do not make `raw_payload`, byte-length metadata, `redaction_version`, `redacted_path_count`, or `raw_payload_hmac` nullable; use `{}` and `0` defaults for payload/metadata where a server default is safe, but require callers to provide the HMAC. Add indexes named in the artifact list, including `postgresql_using="gin"` on `incident_ids` and a functional index on `sa.text("(decision_summary ->> 'no_dispatch_reason')")`. In `downgrade()`, drop every created index in reverse order before dropping `incident_events`. Add `IncidentEvent` to `app/persistence/models.py` with `server_default=sa.func.gen_random_uuid()` and SQLAlchemy imports for `Boolean`, `Integer`, and `sqlalchemy as sa`; keep column names/nullability/defaults aligned with the migration. Add audit settings to `Settings`: `audit_raw_payload_max_bytes: int = Field(default=65_536, ge=1_024, le=1_048_576)` and required `audit_raw_payload_hmac_key: SecretStr`. Add `_require_audit_hmac_key` so missing or blank keys fail and configured audit key equality with `operator_api_token` or `ingress_api_token` fails with an error message naming `audit_raw_payload_hmac_key`. Extend `tests/conftest.py::_SETTINGS_ENV_KEYS` with `CORRELIA_AUDIT_RAW_PAYLOAD_MAX_BYTES` and `CORRELIA_AUDIT_RAW_PAYLOAD_HMAC_KEY`. Update every current test `Settings(...)` callsite/factory and env-only app fixture in the files listed for this task to supply `audit_raw_payload_hmac_key="test-audit-hmac"` or set `CORRELIA_AUDIT_RAW_PAYLOAD_HMAC_KEY` in the fixture; keep tests that intentionally omit the audit key only in the new settings validation block.</action>
  <verify>
    <automated>uv run pytest tests/test_migrations.py tests/test_settings.py -q -k "incident_events or audit or settings_env_keys"</automated>
    <automated>uv run pytest tests/test_config_status_api.py tests/test_exposure_config.py tests/test_health.py tests/test_incidents_api.py tests/test_ingress_router.py tests/test_lifecycle_worker.py tests/test_metrics_api.py tests/test_plugins_router.py tests/test_rate_limit.py tests/test_security.py tests/test_size_limit.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `migrations/versions/0003_create_incident_events.py` exists and has `down_revision = "0002_add_threshold_state"`.
    - `incident_events.id` is a PostgreSQL UUID primary key with server default `gen_random_uuid()`; no production helper is responsible for generating audit ids in Python.
    - `raw_payload`, `raw_payload_original_byte_length`, `raw_payload_stored_byte_length`, `raw_payload_truncated`, `redaction_version`, `redacted_path_count`, and `raw_payload_hmac` are non-null in both migration and `IncidentEvent` model.
    - Migration defines CHECK constraints for event type, severity, incident effect, JSONB object/array shape, non-negative byte lengths, stored length not greater than original length, positive redaction version, and non-negative redacted path count.
    - Migration defines `ix_incident_events_incident_ids_gin` with PostgreSQL GIN and `ix_incident_events_no_dispatch_reason` on `decision_summary ->> 'no_dispatch_reason'`.
    - `IncidentEvent.__tablename__` is exactly `incident_events`, and its field names match the migration column names.
    - `Settings.model_fields` includes `audit_raw_payload_max_bytes` and `audit_raw_payload_hmac_key`; the HMAC key is a required `SecretStr` and is not allowed to equal either configured auth token.
    - `_SETTINGS_ENV_KEYS` includes both audit env vars, and current tests that instantiate `Settings` outside the missing-key validation cases pass a test audit HMAC key or fixture env var.
  </acceptance_criteria>
  <done>Alembic/schema/model/settings contracts are in place, migration and settings tests prove D-04, D-06, D-07, D-13, and the existing Settings-dependent test suite no longer fails because of the new required audit HMAC key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add strict audit domain contracts plus independent redaction, HMAC, and cursor helpers</name>
  <files>app/domain/audit.py, app/persistence/audit.py, tests/test_domain_audit.py, tests/test_audit_redaction.py</files>
  <read_first>
    @.planning/phases/07-incident-event-audit-trail/07-CONTEXT.md
    @.planning/phases/07-incident-event-audit-trail/07-RESEARCH.md
    @.planning/phases/07-incident-event-audit-trail/07-PATTERNS.md
    @app/domain/events.py
    @app/domain/incidents.py
    @app/persistence/incidents.py
    @tests/test_domain_incidents.py
  </read_first>
  <behavior>
    - RED first: `tests/test_domain_audit.py` rejects extra fields, invalid literals, too many incident ids, naive filter timestamps, overlong `decision_reason`, overlong `no_dispatch_reason`, and any response payload that attempts to carry raw payload or a full normalized event field.
    - RED first: `tests/test_audit_redaction.py` proves nested sensitive keys and sensitive string values become `[redacted]`, the HMAC is computed over the pre-redaction canonical JSON, semantic capping keeps serialized stored payload bytes at or below `max_bytes`, and metadata reflects original length, stored length, truncation, version, and redacted leaf count.
    - RED first: cursor tests prove `encode_audit_cursor` and `decode_audit_cursor` round-trip timezone-aware `(accepted_at, id)` and reject invalid payloads or naive datetimes with `ValueError("invalid audit event cursor")`.
  </behavior>
  <action>Create `app/domain/audit.py` with strict Pydantic v2 models using `ConfigDict(strict=True, extra="forbid")`. Define `AuditDecisionSummary` with `schema_version: Literal[1] = 1` and exactly these D-18 fields: `decision_kind`, `incident_effect`, `rule_name`, `group_key`, `incident_ids`, `affected_incident_count`, `incident_ids_truncated`, `decision_reason`, `no_dispatch_reason`, `notification_intent`, `counted_count`, `threshold_count`, `threshold_crossed`, `replay`, `first_threshold_transition`, `recovery_resolution`, and `affected_object_removed`. Use bounded string annotations with 256 chars generally and 128 chars for `no_dispatch_reason`, cap `incident_ids` at 20, and keep the excluded D-19 high-cardinality/rule-internal details out of the model. Define `AuditEventListFilters` with `incident_id`, `has_incident`, `fingerprint`, `source_id`, `event_type`, `incident_effect`, `no_dispatch_reason`, `severity`, `host`, `service`, `accepted_since`, `accepted_until`, `event_timestamp_since`, `event_timestamp_until`, `limit`, `cursor`, and `offset`; validate all timestamp filters are timezone-aware. Define `BoundedResponseMessage` as a response-only string annotation with min length 0 and max length 512. Define `AuditEventResponse` with safe projected fields only: id/timestamps/source/fingerprint/type/severity/host/service/incident ids/effect/decision summary/bounded normalized message/normalized tags/raw-payload metadata/HMAC. Define `AuditEventListResponse` with `items`, `total`, `limit`, `offset`, and `next_cursor`. In `app/persistence/audit.py`, define audit-owned `AUDIT_SENSITIVE_FRAGMENTS`; do not import `DecisionContext` or its private forbidden-fragment tuple. Implement canonical JSON bytes with sorted keys and compact separators. Implement `compute_payload_hmac(payload, hmac_key)` using stdlib `hmac.new(key_bytes, canonical_json_bytes(payload), hashlib.sha256).hexdigest()`, accepting `SecretStr`, `str`, or `bytes` keys. Implement `redact_payload(payload, *, max_bytes, hmac_key)` returning `RedactedPayload`; recursively walk dict/list/scalar values, redact leaves when key text or string value contains an audit sensitive fragment, compute HMAC before redaction, then semantically shrink oversized redacted JSON by replacing or dropping largest string/container leaves until stored JSON bytes are at or below `max_bytes` without cutting JSON bytes. Implement `AuditEventCursor`, `encode_audit_cursor`, and `decode_audit_cursor` mirroring `encode_incident_cursor`/`decode_incident_cursor` but using `accepted_at` and the error string `invalid audit event cursor`.</action>
  <verify>
    <automated>uv run pytest tests/test_domain_audit.py tests/test_audit_redaction.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `AuditDecisionSummary.model_config` is strict and forbids extras; `schema_version` is literal version `1`.
    - `AuditDecisionSummary` contains all D-18 fields and none of the D-19 excluded high-cardinality/rule-internal fields.
    - `AuditEventResponse` has no `raw_payload` field and no full `normalized_event` field; it has `normalized_event_message` capped at 512 chars and `normalized_event_tags` typed through the existing tag constraints; both projected values pass through `redact_normalized_event_message_tags` to scrub sensitive keys/values before serialization.
    - `AuditEventListFilters` validates all accepted/event timestamp range fields as timezone-aware when present.
    - `app/persistence/audit.py` does not import `DecisionContext` and does not import any private forbidden-fragment symbol from `app.domain.incidents`.
    - `redact_payload` records `redaction_version == 1`, non-negative lengths/counts, `stored_byte_length` not greater than `max_bytes`, and `truncated=True` only when semantic capping changed the stored payload to fit.
    - `compute_payload_hmac` changes when the pre-redaction payload changes and stays stable when only redaction output would differ.
    - `decode_audit_cursor` rejects malformed base64/JSON, missing keys, invalid UUIDs, and naive datetimes with `ValueError`.
  </acceptance_criteria>
  <done>Audit domain contracts and pure helper behavior are test-proven for D-04, D-05, D-06, D-07, D-08, D-17, D-18, and D-19 without coupling raw-payload redaction to incident decision-context validation; read-time redaction of normalized_event message/tags is implemented idempotently so it can be called safely from both repository and router layers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add audit repository insert/list helpers and persistence tests</name>
  <files>app/persistence/audit.py, tests/test_audit_persistence.py</files>
  <read_first>
    @.planning/phases/07-incident-event-audit-trail/07-CONTEXT.md
    @.planning/phases/07-incident-event-audit-trail/07-RESEARCH.md
    @.planning/phases/07-incident-event-audit-trail/07-PATTERNS.md
    @app/domain/audit.py
    @app/domain/events.py
    @app/persistence/models.py
    @app/persistence/incidents.py
    @tests/test_incident_repository.py
    @tests/test_migrations.py
  </read_first>
  <behavior>
    - RED first: `tests/test_audit_persistence.py` migrates PostgreSQL with Testcontainers, inserts an audit row through `insert_incident_event`, commits, and reads it back with non-null raw-payload metadata and a server-generated UUID.
    - RED first: list tests seed multiple rows and assert default ordering is `accepted_at DESC, id DESC`, cursor pagination returns the next page from `(accepted_at, id)`, and offset pagination still reports filtered `total` and `offset`.
    - RED first: filter tests cover `incident_id` JSONB containment, `has_incident=False` mapping strictly to `incident_effect == "none"`, `has_incident=True` excluding only `none`, scalar filters, accepted/event timestamp ranges, and `decision_summary["no_dispatch_reason"]` filtering.
    - RED first: safe-projection tests assert list rows do not expose `raw_payload` or the full `normalized_event` document while preserving bounded `normalized_event_message`, `normalized_event_tags`, and metadata fields, and that sensitive fragments in message/tags are replaced with `[redacted]` by `redact_normalized_event_message_tags`.
  </behavior>
  <action>Extend `app/persistence/audit.py` with repository symbols. Define `_AUDIT_LIST_COLUMNS` as an explicit SQLAlchemy projection over `IncidentEvent` that includes id, accepted_at, event_timestamp, source_id, fingerprint, event_type, severity, host, service, incident_ids, incident_effect, decision_summary, raw-payload metadata, `raw_payload_hmac`, `IncidentEvent.normalized_event["message"].astext.label("normalized_event_message")`, and `IncidentEvent.normalized_event["tags"].label("normalized_event_tags")`; do not include `IncidentEvent.raw_payload` and do not include the whole `IncidentEvent.normalized_event` column. Define a private idempotent helper `_redact_normalized_event_message_tags(message, tags)` (or public `redact_normalized_event_message_tags`) that recursively scrubs sensitive keys and string values from the projected message string and tags dict using the same `AUDIT_SENSITIVE_FRAGMENTS` list as `redact_payload`, returning a redacted `(message, tags)` pair suitable for `AuditEventListRow` and `AuditEventResponse`. Apply this helper when mapping projected rows into `AuditEventListRow` inside `list_incident_events`. Define `AuditEventListRow` as the typed row projection and map SQL tuple rows into it. Define `IncidentEventListPage(events, next_cursor, total, offset)` and implement `insert_incident_event(session, normalized_event, raw_payload, decision_summary, ...)` that computes `RedactedPayload` via `redact_payload`, builds an `IncidentEvent` row, and flushes it. Implement `list_incident_events(session, filters)` that applies all D-11 filters, supports cursor/offset pagination, and returns `IncidentEventListPage` of redacted `AuditEventListRow` rows. Keep the repository free of any incident decision logic.</action>
  <verify>
    <automated>uv run pytest tests/test_audit_persistence.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `insert_incident_event` can persist a complete row with non-null raw payload, normalized event, decision summary, source ID, fingerprint, severity, host, service, timestamps, metadata, HMAC, and server-generated UUID.
    - `list_incident_events` returns `IncidentEventListPage` and never returns ORM `IncidentEvent` entities for the default list path.
    - `_AUDIT_LIST_COLUMNS` omits `IncidentEvent.raw_payload` and the full `IncidentEvent.normalized_event` column while projecting only message and tags from normalized_event; the mapped `AuditEventListRow` message and tags have passed through `redact_normalized_event_message_tags`.
    - `incident_id` filter uses JSONB `.contains([str(uuid)])` semantics; tests prove matching and non-matching UUIDs.
    - `has_incident=False` returns only rows with `incident_effect == "none"`; `below_threshold` and `replay` remain no-dispatch reasons rather than no-incident cases.
    - `no_dispatch_reason` filter compares against `decision_summary["no_dispatch_reason"].astext` and returns rows whose summary reason matches.
    - Cursor pagination round-trips through `encode_audit_cursor`/`decode_audit_cursor`, orders by `(accepted_at, id)` descending, and returns a non-null `next_cursor` only when more rows remain.
    - Offset pagination reports filtered `total`, echoes the requested offset, and returns `next_cursor is None`.
    - No Phase 7 foundation code imports `app.persistence.audit` into decision-state modules; ingress/API wiring is left to 07-02/07-03.
  </acceptance_criteria>
  <done>Audit persistence helpers are ready for later ingress/API plans, and Testcontainers-backed tests prove insert, cursor, filters, safe projection, and read-time message/tag redaction behavior for AUD-01/AUD-02 foundation scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Source payload to audit redactor | Accepted source payload content is untrusted and may contain secrets before persistence. |
| Environment/settings to HMAC | `CORRELIA_AUDIT_RAW_PAYLOAD_HMAC_KEY` is secret configuration used to derive integrity metadata. |
| Repository filters/cursors to SQL | Operator-supplied filters and cursors are untrusted inputs to future audit read queries. |
| Audit table to later operator API | Stored raw payload and normalized event are sensitive; default reads must project safe metadata only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-01-I | Information disclosure | `redact_payload`, `incident_events.raw_payload` | mitigate | Recursive audit-owned redaction handles nested keys and string values before persistence, stores redaction metadata, and tests ensure secrets are replaced. |
| T-07-02-D | Denial of service | `redact_payload`, `raw_payload` JSONB | mitigate | Configurable `audit_raw_payload_max_bytes` defaults to 65_536 and semantic capping keeps stored JSON bytes under the limit without byte-truncating JSON. |
| T-07-03-T | Tampering | `raw_payload_hmac` | mitigate | Compute HMAC-SHA256 over canonical pre-redaction payload with a required dedicated SecretStr key that cannot be blank or reused from auth tokens. |
| T-07-04-I | Information disclosure | `list_incident_events` projection | mitigate | Default repository projection omits raw_payload and full normalized_event, selecting only bounded message/tags and metadata for D-08. |
| T-07-05-T | Tampering | `decode_audit_cursor` | mitigate | Cursor decoder validates base64 JSON shape, UUID, and timezone-aware accepted_at, raising `ValueError` on malformed input before SQL use. |
| T-07-06-D | Denial of service | `list_incident_events` filters | mitigate | `AuditEventListFilters.limit` is bounded to 200 and indexed filters include cursor tuple, scalar indexes, GIN incident_ids, and functional no_dispatch_reason. |
| T-07-SC | Tampering | Package installs | accept | No new packages are installed; Phase 7 foundation uses existing dependencies and Python stdlib only. |
</threat_model>

<verification>
Run the task-level verification commands, then run the full foundation gate:

<automated>uv run pytest tests/test_domain_audit.py tests/test_audit_redaction.py tests/test_audit_persistence.py tests/test_migrations.py tests/test_settings.py -q</automated>
<automated>uv run pytest tests/test_config_status_api.py tests/test_exposure_config.py tests/test_health.py tests/test_incidents_api.py tests/test_ingress_router.py tests/test_lifecycle_worker.py tests/test_metrics_api.py tests/test_plugins_router.py tests/test_rate_limit.py tests/test_security.py tests/test_size_limit.py -q</automated>

Manual review is not required for this plan. Confirm no implementation task adds an API router, route classification, ingress transaction refactor, or manager commit refactor; those are assigned to 07-02/07-03.
</verification>

<success_criteria>
- `alembic upgrade head` creates `incident_events` with server-generated UUID ids, non-null raw-payload metadata, CHECK constraints, and all required indexes.
- `IncidentEvent` model matches the migration and is discoverable through `Base.metadata`.
- `AuditDecisionSummary`, `AuditEventListFilters`, `AuditEventResponse`, and `AuditEventListResponse` enforce strict bounded contracts for D-08 and D-17 through D-19.
- `redact_payload` redacts nested sensitive content, computes HMAC over pre-redaction canonical JSON, and semantically caps stored payloads without corrupting JSON.
- `insert_incident_event` and `list_incident_events` provide the foundation for one audit row per accepted normalized event while default list projection excludes `raw_payload` and full `normalized_event`.
- Required audit settings are present, validated, and all existing Settings test factories/calls are updated so unrelated test modules do not fail from missing `CORRELIA_AUDIT_RAW_PAYLOAD_HMAC_KEY`.
- No code in this plan wires audit writes into ingress or exposes `/v1/incident-events`; those remain for dependent plans.
</success_criteria>

<output>
Create `.planning/phases/07-incident-event-audit-trail/07-01-SUMMARY.md` when done.
</output>
