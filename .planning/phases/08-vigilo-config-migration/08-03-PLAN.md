---
phase: "08-vigilo-config-migration"
plan: "08-03"
type: "execute"
wave: 3
depends_on:
  - "08-02"
requirements: [CFG-05, CFG-06, CFG-07]
files_modified:
  - "scripts/migrate_vigilo_config.py"
  - "tests/test_vigilo_config_migration.py"
files_created:
  - "tests/fixtures/vigilo/plugins_with_placeholder_option.yaml"
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "D-05/CFG-05/CFG-06: generated plugins.yaml never contains ${...} or {env: ...}; source email option values using either form fail before staging or promotion."
    - "D-15/CFG-07: if _promote fails after one successful os.replace call, --out-dir returns to its pre-promotion state and newly-promoted generated files are removed."
  artifacts:
    - path: "scripts/migrate_vigilo_config.py"
      provides: "D-05 preflight rejection for ${...} strings and {env: ...} mappings in allowed email option values, plus D-15 mid-promotion rollback."
      exports:
        - "_preflight_plugins"
        - "_transform_plugins"
        - "_promote"
    - path: "tests/test_vigilo_config_migration.py"
      provides: "Regression tests for D-05 placeholder rejection and D-15 partial promotion rollback."
      contains: "test_plugin_option_placeholders_fail_before_output, test_promote_rolls_back_new_files_on_mid_promotion_failure"
    - path: "tests/fixtures/vigilo/plugins_with_placeholder_option.yaml"
      provides: "Vigilo email output fixture containing ${...} string and {env: ...} mapping forms under allowed option keys."
  key_links:
    - from: "scripts/migrate_vigilo_config.py:_preflight_plugins"
      to: "tests/fixtures/vigilo/plugins_with_placeholder_option.yaml"
      via: "Raw Vigilo plugin config is rejected as unsupported_plugin_option / CFG-06 before _transform_plugins can copy values into generated plugins.yaml."
      pattern: "unsupported_plugin_option"
    - from: "scripts/migrate_vigilo_config.py:_promote"
      to: "tests/test_vigilo_config_migration.py::test_promote_rolls_back_new_files_on_mid_promotion_failure"
      via: "monkeypatched os.replace succeeds for rules.yaml then raises on topology.yaml, proving rollback removes newly-promoted output."
      pattern: "os.replace"
---

# Phase 8: Vigilo Config Migration - Plan 3 Gap Closure

**Requirement IDs:** CFG-05, CFG-06, CFG-07

**Gap source:** `.planning/phases/08-vigilo-config-migration/08-VERIFICATION.md`

**Goal:** Close the two failed Phase 8 locked-decision gaps without changing executed plans `08-01`/`08-02` or their SUMMARY files.

## Execution Context

- Gap-closure mode for Phase 8. Do not rewrite or modify `.planning/phases/08-vigilo-config-migration/08-01-PLAN.md`, `08-02-PLAN.md`, `08-01-SUMMARY.md`, or `08-02-SUMMARY.md`.
- Read first: `.planning/phases/08-vigilo-config-migration/08-VERIFICATION.md`, `08-CONTEXT.md`, `08-RESEARCH.md`, `08-PATTERNS.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`.
- Relevant code: `scripts/migrate_vigilo_config.py` lines around `_preflight_plugins`, `_transform_plugins`, and `_promote`; `tests/test_vigilo_config_migration.py`; `tests/fixtures/vigilo/plugins_valid.yaml`.
- Existing behavior to preserve: unknown email option keys still fail as `unsupported_plugin_option`; plaintext credential keys still fail as `plaintext_smtp_credentials`; valid migration fixtures still generate and load successfully.

## Source Coverage Audit

| Source | ID | Gap-closure coverage |
|---|---|---|
| GOAL | Phase 8 config migration | Restores the promised fail-closed plugin translation and no-partial-output behavior for the migration CLI. |
| REQ | CFG-05 | Task 1 prevents generated email output plugin config from carrying unsupported `${...}` or `{env: ...}` forms. |
| REQ | CFG-06 | Task 1 reports these unsupported plugin option values as `unsupported_plugin_option` with `requirement="CFG-06"` before output writes. |
| REQ | CFG-07 | Task 2 preserves staged-then-promoted output semantics when promotion itself fails after validation. |
| RESEARCH | Pitfall: environment placeholders in plugins.yaml | Task 1 follows the research warning that Correlia does not expand these forms and must not emit them. |
| RESEARCH | Pitfall: partial output on failure | Task 2 closes the mid-promotion branch that validation-failure tests did not exercise. |
| CONTEXT | D-05 | Task 1 rejects exact `${...}` strings and `{env: ...}` mappings in allowed email plugin option values. |
| CONTEXT | D-15 | Task 2 removes newly-promoted generated files when a later promotion step raises. |
| VERIFICATION | Gap 1 | Task 1 addresses the failed D-05 truth from `08-VERIFICATION.md` lines 9-23. |
| VERIFICATION | Gap 2 | Task 2 addresses the failed D-15 promotion truth from `08-VERIFICATION.md` lines 24-38. |
| CONTEXT | Other D-01..D-04, D-06..D-13 | Already verified in `08-VERIFICATION.md`; out of scope for this focused gap-closure plan. |

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Reject D-05 plugin option placeholder syntax before transform</name>
  <files>scripts/migrate_vigilo_config.py, tests/test_vigilo_config_migration.py, tests/fixtures/vigilo/plugins_with_placeholder_option.yaml</files>
  <behavior>
    - RED: A Vigilo email plugin fixture with `smtp_host: ${SMTP_HOST}` and a `to_addresses` item shaped as `{env: OPS_EMAIL}` fails the migration before staging or promotion.
    - GREEN: `_preflight_plugins` recursively scans allowed email config values and lists, reports `unsupported_plugin_option` / `CFG-06` for `${...}` strings and `{env: ...}` mappings, and `_transform_plugins` never emits those values.
    - Regression: Existing valid email plugin migration and existing unknown-option / plaintext-credential failures keep their current codes.
  </behavior>
  <action>Per D-05, add the fixture `tests/fixtures/vigilo/plugins_with_placeholder_option.yaml` by copying the valid email output shape and replacing allowed option values with both exact rejected forms: `smtp_host: ${SMTP_HOST}` and a nested `to_addresses` item using `{env: OPS_EMAIL}`. Add a dedicated test named `test_plugin_option_placeholders_fail_before_output` before production changes; it must run the CLI through `_run_cli`, assert a non-zero result, assert `report["ok"] is False`, assert `report["generated"] is None`, assert no `rules.yaml`, `topology.yaml`, or `plugins.yaml` appears under `out_dir`, and assert `unsupported_plugin_option` / `CFG-06` errors identify both the `${...}` string location and the `{env: ...}` mapping location. Also add the fixture to `UNSUPPORTED_FIELD_CASES` so the parametrized unsupported-field test covers the catalog row. Then implement a small recursive preflight helper used by `_preflight_plugins` only for keys in `_ALLOWED_EMAIL_CONFIG_KEYS`: scan strings for `${...}` anywhere in the value, scan lists recursively, and scan mappings for an `env` key before recursing into their values. Report each hit as `MigrationIssue(domain="plugins", code="unsupported_plugin_option", requirement="CFG-06")` with a location rooted at `plugins.outputs.NAME.config.KEY` and index/key suffixes for nested values. Do not expand, rewrite, redact into generated config, or accept `{env: ...}` as a secret-reference type; first-class secret references are explicitly deferred in `08-CONTEXT.md`.</action>
  <verify>
    <automated>uv run pytest tests/test_vigilo_config_migration.py::test_plugin_option_placeholders_fail_before_output -x</automated>
    <automated>uv run pytest tests/test_vigilo_config_migration.py::test_unsupported_fields_fail -k plugins_with_placeholder_option -x</automated>
    <automated>uv run pytest tests/test_vigilo_config_migration.py::test_migrate_email_plugin tests/test_vigilo_config_migration.py::test_unsupported_fields_fail -k "plugins_with_unknown_email_option or plugins_with_credentials or plugins_with_placeholder_option" -x</automated>
  </verify>
  <done>D-05 is closed when the new fixture proves `${...}` and `{env: ...}` in allowed email plugin option values fail as `unsupported_plugin_option` / `CFG-06`, no generated files are promoted on that failure, and existing valid/credential/unknown-option plugin tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Roll back newly-promoted files on D-15 mid-promotion failure</name>
  <files>scripts/migrate_vigilo_config.py, tests/test_vigilo_config_migration.py</files>
  <behavior>
    - RED: A direct `_promote` regression test monkeypatches `os.replace` to succeed for `rules.yaml` and raise on the next file; before the fix, `out_dir/rules.yaml` remains as partial generated output.
    - GREEN: `_promote` tracks successful replacements and removes newly-promoted targets without backups when a later replacement raises, while still restoring backed-up pre-existing targets.
    - Regression: Normal CLI generation still writes all three files, and validation failures before promotion still leave existing `out_dir` contents untouched.
  </behavior>
  <action>Per D-15, add a focused test named `test_promote_rolls_back_new_files_on_mid_promotion_failure` before production changes. Import the migration module so the test can monkeypatch `scripts.migrate_vigilo_config.os.replace`; create a staging directory containing `rules.yaml`, `topology.yaml`, and `plugins.yaml`; create an empty `out_dir`; monkeypatch `os.replace` to call the real replacement for the first destination `rules.yaml` and then raise `OSError` for the next destination. Assert `_promote(staging, out_dir)` raises and assert `out_dir` contains none of the generated YAML files afterward. Then update `_promote` to track the names whose `os.replace` call completed. In the exception handler, for each successfully promoted name without a backup, unlink the target if it exists; for names with backups, restore the backup as the existing code does. Keep the cleanup best-effort and re-raise the original exception. Do not change the staging, validation, report, or transform flow; this task only closes the mid-promotion rollback branch.</action>
  <verify>
    <automated>uv run pytest tests/test_vigilo_config_migration.py::test_promote_rolls_back_new_files_on_mid_promotion_failure -x</automated>
    <automated>uv run pytest tests/test_vigilo_config_migration.py::test_cli_generates_files tests/test_vigilo_config_migration.py::test_validation_failure_leaves_existing_out_dir_untouched -x</automated>
  </verify>
  <done>D-15 is closed when a simulated partial promotion leaves no generated YAML files in an originally empty `out_dir`, normal successful promotion still writes all three files, and existing validation-failure preservation behavior remains green.</done>
</task>
</tasks>

<acceptance_criteria>
- `tests/fixtures/vigilo/plugins_with_placeholder_option.yaml` exists and contains both `${...}` and `{env: ...}` under allowed email option values.
- `test_plugin_option_placeholders_fail_before_output` proves the CLI fails before output promotion with `unsupported_plugin_option` / `CFG-06` for both exact D-05 forms.
- `_preflight_plugins` recursively scans allowed email option values, including list items and mappings, rather than only checking `smtp_host` strings.
- `_transform_plugins` continues to map only accepted values and does not emit `${...}` or `{env: ...}`.
- `test_promote_rolls_back_new_files_on_mid_promotion_failure` monkeypatches `os.replace` so `rules.yaml` promotion succeeds and the next promotion raises, then asserts no generated YAML remains in `out_dir`.
- `_promote` preserves existing backup restore behavior and additionally unlinks newly-promoted files that had no pre-existing backup.
- No unrelated Phase 8 work, documentation, executed plans, or SUMMARY files are changed.
</acceptance_criteria>

<verify>
<automated>uv run pytest tests/test_vigilo_config_migration.py::test_plugin_option_placeholders_fail_before_output tests/test_vigilo_config_migration.py::test_promote_rolls_back_new_files_on_mid_promotion_failure -x</automated>
<automated>uv run pytest tests/test_vigilo_config_migration.py -x</automated>
</verify>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| operator Vigilo plugins YAML -> migration preflight | Maintainer-supplied plugin values may contain unsupported placeholder syntax that would otherwise be emitted as literal runtime config. |
| staging directory -> `--out-dir` | Validated generated files cross from temporary staging into operator-visible config files through multiple filesystem replacements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-08-03-01 | Information disclosure | `_preflight_plugins` / `_transform_plugins` | mitigate | Reject `${...}` and `{env: ...}` in allowed email option values before staging so generated `plugins.yaml` cannot contain unsupported secret-reference or interpolation syntax. |
| T-08-03-02 | Tampering | `_promote` | mitigate | Track completed `os.replace` calls and remove newly-promoted targets on later failure while restoring backed-up pre-existing files. |
| T-08-03-03 | Repudiation | migration failure report | accept | Existing report shape already records structured `code`, `location`, and `requirement`; this gap closure reuses `unsupported_plugin_option` / `CFG-06` rather than adding a new report surface. |
</threat_model>

## PLAN COMPLETE
