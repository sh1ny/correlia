---
phase: "08-vigilo-config-migration"
plan: "02"
type: "execute"
wave: 2
depends_on:
  - "08-01"
requirements: [CFG-01, CFG-02, CFG-03, CFG-04, CFG-05, CFG-06, CFG-07]
files_modified:
  - "CONFIGURATION.md"
files_created:
  - "scripts/migrate_vigilo_config.py"
  - "tests/test_vigilo_config_migration.py"
  - "tests/fixtures/vigilo/rules_valid.yaml"
  - "tests/fixtures/vigilo/rules_min_hosts.yaml"
  - "tests/fixtures/vigilo/rules_is_dc_level.yaml"
  - "tests/fixtures/vigilo/rules_empty_actions.yaml"
  - "tests/fixtures/vigilo/topology_valid.yaml"
  - "tests/fixtures/vigilo/plugins_valid.yaml"
  - "tests/fixtures/vigilo/plugins_with_credentials.yaml"
autonomous: true
must_haves:
  truths:
    - "CFG-01/D-01/D-02/D-03: Maintainers can invoke scripts/migrate_vigilo_config.py with exactly one YAML file for --rules, --topology, --plugins, and an --out-dir."
    - "CFG-02: Supported Vigilo rules become Correlia rules with inverted rank priorities, fnmatch-translated host/service patterns, topology-prefixed tag keys, rewritten summary placeholders, and create_incident action objects."
    - "CFG-03/CFG-04/D-10/D-11: Supported Vigilo topology becomes strict hostname/subnet topology YAML, including tag_capture_groups for hostname regex capture group 1."
    - "CFG-05/D-04/D-05/D-06: Supported Vigilo email outputs become Correlia email output entries without emitting or copying plaintext SMTP credentials or environment-reference syntax."
    - "CFG-06/D-12/D-13: Unsupported Vigilo semantics are preflight-scanned, aggregated across all inputs, reported clearly, and cause a non-zero exit before output promotion."
    - "CFG-07/D-14/D-15: Generated YAML is staged, validated through Correlia loaders plus email plugin instantiation, and only then promoted without partial output on failure."
  artifacts:
    - path: "scripts/migrate_vigilo_config.py"
      provides: "Standalone Vigilo/VDE-to-Correlia config migration CLI."
      exports:
        - "main"
    - path: "tests/test_vigilo_config_migration.py"
      provides: "CLI, transform, failure aggregation, atomic output, and loader round-trip tests."
      contains: "test_cli_generates_files, test_migrate_rules, test_migrate_topology, test_migrate_email_plugin, test_unsupported_fields_fail, test_generated_files_load_and_instantiate"
    - path: "tests/fixtures/vigilo/"
      provides: "Representative valid and unsupported Vigilo YAML inputs."
    - path: "CONFIGURATION.md"
      provides: "Operator-facing migration notes for priority inversion, capture-group topology, credential rejection, and validation."
  key_links:
    - from: "scripts/migrate_vigilo_config.py"
      to: "app.config.plugins.load_plugin_registry_config"
      via: "Generated plugins.yaml is validated first so generated plugin names can constrain rule actions."
      pattern: "load_plugin_registry_config"
    - from: "scripts/migrate_vigilo_config.py"
      to: "app.plugins.loader.load_plugin_registry"
      via: "Generated fixed email class is instantiated to catch SmtpOutputOptions validation that config loading alone misses."
      pattern: "load_plugin_registry"
    - from: "scripts/migrate_vigilo_config.py"
      to: "app.config.rules.load_rules_config"
      via: "Generated rules.yaml is validated with known_plugins from generated output plugins."
      pattern: "known_plugins"
    - from: "scripts/migrate_vigilo_config.py"
      to: "app.config.topology.load_topology_config"
      via: "Generated topology.yaml is validated after 08-01 tag_capture_groups support exists."
      pattern: "load_topology_config"
---

# Phase 8: Vigilo Config Migration - Plan 2

**Requirement IDs:** CFG-01, CFG-02, CFG-03, CFG-04, CFG-05, CFG-06, CFG-07

**Goal:** Build the standalone Vigilo/VDE config migration CLI that translates supported YAML into validated Correlia `rules.yaml`, `topology.yaml`, and `plugins.yaml`, while failing clearly on unsupported semantics and credentials.

## Execution Context

- Depends on Plan 08-01 for `HostnameTopologyRule.tag_capture_groups` and enrichment support.
- Read first: `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`, `.planning/phases/08-vigilo-config-migration/08-CONTEXT.md`, `.planning/phases/08-vigilo-config-migration/08-RESEARCH.md`, `.planning/phases/08-vigilo-config-migration/08-PATTERNS.md`.
- Code analogs: `app/main.py` loader order, `app/config/rules.py`, `app/config/topology.py`, `app/config/plugins.py`, `app/plugins/loader.py`, `app/plugins/outputs/email.py`, `tests/test_rule_topology_yaml.py`, `tests/test_plugin_registry.py`, `tests/test_topology_enrichment.py`.

## Planner Decisions Resolved

- Priority inversion: sort Vigilo rules by descending integer `priority` and assign Correlia priorities `1..N` in that order, so Correlia's ascending `RuleEngine` preserves Vigilo's higher-first evaluation. Reject duplicate or missing Vigilo priorities rather than inventing tie semantics.
- Report format: print human-readable errors to stderr and support optional `--report-path` JSON. The JSON shape is `{ok: bool, errors: [{domain, location, code, message, requirement}], generated: {rules, topology, plugins} | null}`. Do not log raw YAML values or credential values.
- `tag_capture_groups`: consume the Plan 08-01 default-empty mapping. The migration emits `tag_capture_groups` only for hostname patterns with capture groups plus `target_tag`; literal-only generated hostname rules may omit the field.
- Unsupported-field catalog: maintain module-level constants in `scripts/migrate_vigilo_config.py` and reject every item listed in the catalog below during preflight, before transformation or output writes.
- SMTP TLS assumption: treat VDE `use_tls: true` or omitted `use_tls` as Correlia `start_tls: true`; treat explicit `use_tls: false` as `start_tls: false`. Document this in CLI help and `CONFIGURATION.md`.

## Unsupported-Field Catalog

Preflight must aggregate these as CFG-06 errors:

1. Rules: `rules[].window.min_hosts` presence, `rules[].is_dc_level` presence, missing/non-list/empty `match.severity`, missing/non-int `priority`, duplicate priorities, empty `actions`, non-string action entries, tag match values containing `*`, `?`, `[`, or `]`, tag keys that cannot be converted to valid `topology.*` keys, and `output_summary` placeholders that are neither known normalized fields nor valid topology tag references after rewrite.
2. Topology: hostname entries with capture groups but no `target_tag`, hostname entries with no capture groups and no literal `tags` mapping to preserve, subnet entries missing `cidr`, `value`, or `target_tag`, and topology tag names that cannot be converted to `topology.*` keys.
3. Plugins: any top-level section other than `outputs` including `task_runner`, `inputs`, `llm`, `enrichers`, or `processor`; any output that is not an email output; any source class/module that cannot be mapped to the fixed generated class `app.plugins.outputs.email.SmtpOutputPlugin`; and plaintext SMTP credential keys `smtp_username`, `smtp_password`, `username`, or `password` under output config.
4. Inputs: directories, globs, missing files, and non-`.yaml`/`.yml` paths for `--rules`, `--topology`, or `--plugins` per D-01.

## Source Coverage Audit

| Source | ID | Plan Coverage |
|---|---|---|
| GOAL | Phase 8 config migration | Builds the migration command, strict transforms, fail-closed unsupported reporting, and validation/promote path. |
| REQ | CFG-01 | CLI flags and output files are implemented and tested. |
| REQ | CFG-02 | Rule transform covers severities, host/service patterns, topology tags, summaries, priorities, and actions. |
| REQ | CFG-03 | Topology transform covers hostname and subnet outputs. |
| REQ | CFG-04 | Dynamic hostname topology emits Plan 08-01 `tag_capture_groups`; unsupported dynamic topology fails clearly if it lacks a target tag. |
| REQ | CFG-05 | Email output transform emits strict Correlia output plugin config without plaintext credentials. |
| REQ | CFG-06 | Preflight catalog, aggregated JSON/stderr report, and non-zero exit cover unsupported semantics. |
| REQ | CFG-07 | Staged files validate through config loaders and generated email plugin instantiation before success. |
| RESEARCH | Pitfall 1 | Priority inversion is rank-based and documented. |
| RESEARCH | Pitfall 3 | Validation includes `load_plugin_registry` instantiation, not only `load_plugin_registry_config`. |
| RESEARCH | Pitfall 4 | Wildcard tag values are rejected. |
| RESEARCH | Pitfall 5 | Bare topology placeholders are rewritten to `topology.*`. |
| RESEARCH | Pitfall 6 | Staging and promotion avoid partial output on transform/validation failure. |
| CONTEXT | D-01 | Input path validation rejects directories, globs, missing files, and non-YAML files. |
| CONTEXT | D-02 | The script mirrors single-file config loader and settings path shape. |
| CONTEXT | D-03 | Multi-file and directory sources are rejected. |
| CONTEXT | D-04 | Plaintext SMTP credentials fail closed. |
| CONTEXT | D-05 | Generated `plugins.yaml` contains no environment-reference syntax. |
| CONTEXT | D-06 | Credential handling is outside the migration artifact; plugin skeleton is generated only on success. |
| CONTEXT | D-07/D-08/D-09 | Consumes the `tag_capture_groups` schema and enrichment semantics from Plan 08-01. |
| CONTEXT | D-10 | Hostname capture group plus target tag maps to `topology.<target_tag>: 1`. |
| CONTEXT | D-11 | Subnet topology uses literal tags only. |
| CONTEXT | D-12 | Raw preflight scan detects unsupported fields before transformation. |
| CONTEXT | D-13 | Errors are aggregated across all input files and domains. |
| CONTEXT | D-14 | Files are staged, validated, then promoted. |
| CONTEXT | D-15 | Failed migrations do not leave partial generated files. |

must_haves:
- `scripts/migrate_vigilo_config.py --help` documents `--rules`, `--topology`, `--plugins`, `--out-dir`, optional `--report-path`, priority inversion, and credential rejection.
- A successful migration writes exactly `rules.yaml`, `topology.yaml`, and `plugins.yaml` into `--out-dir`; it does not write `.env` files or credential reference syntax.
- The generated files pass `load_plugin_registry_config`, generated email plugin instantiation via `load_plugin_registry`, `load_rules_config(..., known_plugins=...)`, and `load_topology_config` before success is reported.
- Unsupported fields across rules, topology, and plugins produce one aggregated report and a non-zero exit; no generated files are promoted on failure.
- Tests prove supported rule, topology, email plugin, unsupported-field, credential, path-validation, and atomic-failure behavior.

<action>
1. Add migration fixtures and failing tests first.
   - Create `tests/fixtures/vigilo/rules_valid.yaml` with at least three rules whose Vigilo priorities are `100`, `50`, and `10`, plus severities, host/service fnmatch patterns, topology tag matches, summary placeholders, windows, and string actions referencing `email-ops`.
   - Create unsupported rule fixtures for `min_hosts`, `is_dc_level`, and `actions: []`.
   - Create `tests/fixtures/vigilo/topology_valid.yaml` with one hostname pattern using `^([a-z0-9]+)-prd-.*` plus `target_tag: datacenter`, one literal hostname-tag entry without capture groups, and one subnet entry with `cidr`, `value`, and `target_tag`.
   - Create `tests/fixtures/vigilo/plugins_valid.yaml` with an email output named `email-ops`, no credential keys, SMTP host/port, addresses, subject prefix, and `use_tls` coverage.
   - Create `tests/fixtures/vigilo/plugins_with_credentials.yaml` containing `smtp_username` and `smtp_password` so D-04 fails closed.
   - Create `tests/test_vigilo_config_migration.py` with tests named `test_cli_generates_files`, `test_migrate_rules`, `test_migrate_topology`, `test_migrate_email_plugin`, `test_unsupported_fields_fail`, `test_generated_files_load_and_instantiate`, `test_invalid_input_paths_fail_before_output`, and `test_validation_failure_leaves_existing_out_dir_untouched`.
   - Tests may call `scripts/migrate_vigilo_config.py` through `subprocess.run([sys.executable, ...])` for CLI behavior and may import helper functions only for narrowly focused transform assertions. Keep database/testcontainers out of this test module.
2. Implement `scripts/migrate_vigilo_config.py`.
   - Use only stdlib plus existing project dependencies: `argparse`, `json`, `fnmatch`, `os`, `re`, `shutil`, `sys`, `tempfile`, `dataclasses`, `pathlib.Path`, `yaml.safe_load`, and `yaml.safe_dump(sort_keys=True)`.
   - Before importing any `app.*` module, compute the repository root from `Path(__file__).resolve().parents[1]` and insert its string into `sys.path` if absent, so direct `python scripts/migrate_vigilo_config.py` execution can resolve `app.config.*` when `sys.path[0]` is `scripts/`. Keep the subprocess tests invoking the script file path to prove this path setup.
   - Define `main(argv: Sequence[str] | None = None) -> int` and `if __name__ == "__main__": raise SystemExit(main())` so the script works by direct `python scripts/migrate_vigilo_config.py` invocation.
   - Parse required `--rules`, `--topology`, `--plugins`, and `--out-dir`; parse optional `--report-path`. Do not add flags for multi-file sources or credential references per D-03 and D-05.
   - Validate each input path per D-01: exactly one path, suffix `.yaml` or `.yml`, no glob metacharacters, existing regular file, and not a directory. Report path errors in the same aggregated report format.
   - Load raw YAML as plain dictionaries, then run preflight over all three domains before any transform or output write. Accumulate `MigrationIssue` records with `domain`, `location`, `code`, `message`, and `requirement`, and return exit code `1` when any issue exists.
   - Transform rules per CFG-02: invert priority by descending Vigilo priority to sequential Correlia ranks; rename `match.severity` to `match.severities`; translate host/service fnmatch patterns with `fnmatch.translate()`; prefix tag keys with `topology.` when absent; reject wildcard tag values; rewrite bare non-normalized summary placeholders to `topology.<name>`; map string actions to `{name: "create_incident", plugin: <action>}`.
   - Transform topology per CFG-03/CFG-04: generate stable slug ids from names with collision suffixes; map hostname regex to `hostname_pattern`; when the compiled hostname regex has capture groups and `target_tag`, emit `tags: {}` and `tag_capture_groups: {topology.<target_tag>: 1}` per D-10; when no capture groups exist, preserve literal tags only; map subnet `cidr`/`value`/`target_tag` to Correlia `subnet_rules` with literal `tags` per D-11.
   - Transform plugins per CFG-05: accept only output email plugins; generate `plugin_type: "email"` and fixed `class_path: "app.plugins.outputs.email.SmtpOutputPlugin"`; map `smtp_host` to `host`, `smtp_port` to `port`, pass through addresses/subject, and map `use_tls` to `start_tls` using the planner decision above. Never copy `smtp_username`, `smtp_password`, `username`, or `password`; never emit environment-reference syntax.
   - Stage generated `rules.yaml`, `topology.yaml`, and `plugins.yaml` in a temporary directory under the `--out-dir` parent. Validate staged files by calling `load_plugin_registry_config`, then `load_plugin_registry` on the generated registry to instantiate the generated email plugin, then `load_rules_config(staged_rules, known_plugins=frozenset(registry.names))`, then `load_topology_config`.
   - Promote staged files only after validation succeeds. Use `os.replace` for target-file promotion and restore any pre-existing target files from a backup temp directory if promotion raises, so failure does not leave mixed generated files in `--out-dir` per D-14/D-15.
   - Write a JSON report to `--report-path` when supplied on both success and failure. Print concise human-readable errors to stderr on failure and a concise success line to stdout on success. Do not include raw YAML payloads or credential values in either output.
3. Update operator documentation and run the phase checks.
   - In `CONFIGURATION.md`, update the migration-command section and porting notes to describe the new CLI, priority inversion from Vigilo higher-first to Correlia lower-first ranks, `tag_capture_groups` instead of literal backreference strings, credential fail-closed behavior, unsupported-field failures, and validation through Correlia loaders plus generated email plugin instantiation.
   - Remove or revise any existing notes that imply `\1` should be stored as a literal topology tag value or that generated plugin YAML may contain credential reference syntax.
   - Keep documentation limited to Phase 8 migration behavior; do not add new runtime plugin types, deployment packaging, metrics, API routes, or secret-reference support.
</action>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add migration fixtures and CLI tests</name>
  <files>tests/test_vigilo_config_migration.py, tests/fixtures/vigilo/rules_valid.yaml, tests/fixtures/vigilo/rules_min_hosts.yaml, tests/fixtures/vigilo/rules_is_dc_level.yaml, tests/fixtures/vigilo/rules_empty_actions.yaml, tests/fixtures/vigilo/topology_valid.yaml, tests/fixtures/vigilo/plugins_valid.yaml, tests/fixtures/vigilo/plugins_with_credentials.yaml</files>
  <action>Create the valid and unsupported Vigilo fixtures plus subprocess-based tests named in the numbered action block. Cover CFG-01 through CFG-07 behavior before implementing the CLI, including priority inversion, capture-group topology output, email plugin output without credentials, aggregated unsupported errors, invalid path rejection, loader/plugin validation, and untouched `--out-dir` on failure.</action>
  <verify>
    <automated>uv run pytest tests/test_vigilo_config_migration.py -x</automated>
  </verify>
  <done>The test module proves the desired migration contract and fails against the repository before `scripts/migrate_vigilo_config.py` exists.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement the migration CLI</name>
  <files>scripts/migrate_vigilo_config.py</files>
  <action>Implement `main`, direct-invocation repository-root path setup before `app.*` imports, strict D-01 input validation, raw YAML preflight with the explicit unsupported catalog, rule/topology/plugin transforms, JSON and stderr reporting, temporary staging, Correlia loader validation, generated email plugin instantiation, and D-14/D-15 promotion with backup restore.</action>
  <verify>
    <automated>uv run pytest tests/test_vigilo_config_migration.py -x</automated>
    <automated>uv run python scripts/migrate_vigilo_config.py --help</automated>
  </verify>
  <done>The CLI can be invoked by file path, translates valid fixtures into strict Correlia YAML, rejects unsupported semantics with aggregated reports, and never promotes failed output.</done>
</task>
<task type="auto">
  <name>Task 3: Document migration behavior and run phase checks</name>
  <files>CONFIGURATION.md</files>
  <action>Update only Phase 8 migration notes in `CONFIGURATION.md` so the documented command, priority rank inversion, `tag_capture_groups` topology syntax, SMTP credential failure mode, unsupported-field catalog, and CFG-07 validation sequence match the implemented CLI.</action>
  <verify>
    <automated>uv run pytest tests/test_topology_enrichment.py tests/test_vigilo_config_migration.py -x</automated>
  </verify>
  <done>Operator-facing configuration documentation no longer contradicts D-04 through D-06 or D-07 through D-11, and focused Phase 8 tests pass.</done>
</task>
</tasks>

<acceptance_criteria>
- Running the CLI with valid fixture inputs returns exit code `0`, writes non-empty `rules.yaml`, `topology.yaml`, and `plugins.yaml`, and prints a success message without secrets.
- Generated rule priorities from Vigilo `100`, `50`, and `10` are Correlia `1`, `2`, and `3` respectively, preserving Vigilo evaluation order under Correlia's ascending rule engine.
- Generated rules use strict Correlia shape: `match.severities`, regex host/service patterns, `topology.*` tag keys, rewritten summary placeholders, and action objects with `name: create_incident` plus the generated plugin name.
- Generated topology includes `tag_capture_groups: {topology.datacenter: 1}` for the capture-group hostname fixture and literal subnet tags for subnet fixtures.
- Generated plugin config contains the fixed email class path, valid `SmtpOutputOptions`, no plaintext credential keys, and no environment-reference syntax.
- Unsupported fields from multiple input files appear together in one structured report, and the CLI exits non-zero without promoting generated files.
- Invalid input paths are rejected before transformation and before any output files are written.
- Generated files validate through `load_plugin_registry_config`, generated email plugin instantiation, `load_rules_config` with `known_plugins`, and `load_topology_config`.
- `CONFIGURATION.md` matches the implemented migration behavior and does not contradict D-04 through D-06 or D-07 through D-11.
</acceptance_criteria>

<verify>
<automated>uv run pytest tests/test_vigilo_config_migration.py -x</automated>
<automated>uv run pytest tests/test_topology_enrichment.py tests/test_vigilo_config_migration.py -x</automated>
<automated>uv run python scripts/migrate_vigilo_config.py --help</automated>
</verify>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| operator YAML -> migration CLI | Maintainer-supplied YAML controls migration input and may contain unsupported semantics or plaintext credentials. |
| source plugin config -> generated plugin config | Source plugin metadata is translated into a fixed Correlia output plugin entry. |
| staging directory -> `--out-dir` | Generated artifacts cross from temporary validation space into operator-visible config files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-08-02-01 | Information disclosure | SMTP credential handling | mitigate | Preflight rejects plaintext SMTP credential keys and report output never includes raw credential values; generated `plugins.yaml` omits credential fields. |
| T-08-02-02 | Tampering | Output promotion | mitigate | Write to temp, validate all generated files, then promote with `os.replace` plus backup restore on promotion failure so `--out-dir` is not left partial on migration errors. |
| T-08-02-03 | Elevation of privilege | Plugin class translation | mitigate | Ignore source class paths except to confirm they are mappable to email output; generate the fixed allowlisted `app.plugins.outputs.email.SmtpOutputPlugin` class and instantiate only that generated class during validation. |
| T-08-02-04 | Input validation | Rules/topology/plugins transforms | mitigate | Preflight raw dictionaries for unsupported semantics, then reuse strict Correlia loaders and generated email plugin instantiation as the final validation gate. |
</threat_model>

## PLAN COMPLETE
